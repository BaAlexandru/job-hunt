---
phase: 16-data-stores-on-k8s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/k8s/base/storage-class.yaml
  - infra/k8s/base/postgres/statefulset.yaml
  - infra/k8s/base/minio/statefulset.yaml
  - infra/k8s/base/redis/statefulset.yaml
  - infra/k8s/base/redis/service.yaml
  - infra/k8s/base/kustomization.yaml
  - infra/k8s/overlays/staging/kustomization.yaml
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "All three data stores use local-path-retain StorageClass with reclaimPolicy Retain"
    - "Redis is a StatefulSet with RDB persistence enabled and 1Gi PVC"
    - "PostgreSQL and MinIO PVCs reference local-path-retain StorageClass"
    - "All StatefulSets have persistentVolumeClaimRetentionPolicy set to Retain"
    - "Kustomize build succeeds for both staging and prod overlays"
  artifacts:
    - path: "infra/k8s/base/storage-class.yaml"
      provides: "Custom StorageClass with Retain reclaim policy"
      contains: "reclaimPolicy: Retain"
    - path: "infra/k8s/base/redis/statefulset.yaml"
      provides: "Redis StatefulSet with RDB persistence"
      contains: "kind: StatefulSet"
    - path: "infra/k8s/base/postgres/statefulset.yaml"
      provides: "PostgreSQL with local-path-retain and retention policy"
      contains: "local-path-retain"
    - path: "infra/k8s/base/minio/statefulset.yaml"
      provides: "MinIO with local-path-retain and retention policy"
      contains: "local-path-retain"
  key_links:
    - from: "infra/k8s/base/redis/statefulset.yaml"
      to: "infra/k8s/base/storage-class.yaml"
      via: "storageClassName reference in volumeClaimTemplates"
      pattern: "storageClassName: local-path-retain"
    - from: "infra/k8s/base/kustomization.yaml"
      to: "infra/k8s/base/redis/statefulset.yaml"
      via: "resources list"
      pattern: "redis/statefulset.yaml"
---

<objective>
Create the storage foundation for all data stores: custom StorageClass with Retain policy, convert Redis from Deployment to StatefulSet with RDB persistence, and update PostgreSQL/MinIO StatefulSets to use the new StorageClass with explicit retention policies.

Purpose: Data must survive pod restarts and accidental PVC deletion. The custom StorageClass with reclaimPolicy Retain plus StatefulSet-level persistentVolumeClaimRetentionPolicy provides two layers of protection for all persistent data.

Output: StorageClass manifest, Redis StatefulSet (replacing Deployment), updated PostgreSQL/MinIO StatefulSets, updated kustomization files.
</objective>

<execution_context>
@D:/dev/projects/job-hunt/.claude/get-shit-done/workflows/execute-plan.md
@D:/dev/projects/job-hunt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-data-stores-on-k8s/16-CONTEXT.md
@.planning/phases/16-data-stores-on-k8s/16-RESEARCH.md

<interfaces>
<!-- Current state of files being modified -->

From infra/k8s/base/kustomization.yaml:
```yaml
resources:
  - redis/deployment.yaml    # Will change to redis/statefulset.yaml
```

From infra/k8s/base/postgres/statefulset.yaml:
```yaml
volumeClaimTemplates:
  - spec:
      storageClassName: local-path   # Will change to local-path-retain
```

From infra/k8s/base/minio/statefulset.yaml:
```yaml
volumeClaimTemplates:
  - spec:
      storageClassName: local-path   # Will change to local-path-retain
```

From infra/k8s/base/redis/deployment.yaml:
```yaml
kind: Deployment   # Will be replaced entirely by StatefulSet
# No volumeClaimTemplates — needs to be added
```

From infra/k8s/base/redis/service.yaml:
```yaml
spec:
  type: ClusterIP   # Will change to clusterIP: None (headless)
```

From infra/k8s/overlays/staging/kustomization.yaml:
```yaml
replicas:
  - name: redis    # Works for both Deployment and StatefulSet by name
    count: 0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StorageClass and update PostgreSQL/MinIO StatefulSets</name>
  <files>
    infra/k8s/base/storage-class.yaml,
    infra/k8s/base/postgres/statefulset.yaml,
    infra/k8s/base/minio/statefulset.yaml
  </files>
  <read_first>
    infra/k8s/base/postgres/statefulset.yaml,
    infra/k8s/base/minio/statefulset.yaml,
    .planning/phases/16-data-stores-on-k8s/16-RESEARCH.md
  </read_first>
  <action>
1. Create `infra/k8s/base/storage-class.yaml` with this exact content:
```yaml
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-path-retain
provisioner: rancher.io/local-path
reclaimPolicy: Retain
volumeBindingMode: WaitForFirstConsumer
```
Important: Do NOT add `storageclass.kubernetes.io/is-default-class: "true"` annotation. This StorageClass is explicit-only.

2. Modify `infra/k8s/base/postgres/statefulset.yaml`:
- Add `persistentVolumeClaimRetentionPolicy` under `spec:` (same level as `serviceName`, `replicas`):
```yaml
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
```
- Change `storageClassName: local-path` to `storageClassName: local-path-retain` in volumeClaimTemplates

3. Modify `infra/k8s/base/minio/statefulset.yaml`:
- Add `persistentVolumeClaimRetentionPolicy` under `spec:` (same level as `serviceName`, `replicas`):
```yaml
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
```
- Change `storageClassName: local-path` to `storageClassName: local-path-retain` in volumeClaimTemplates
- Pin the MinIO image from `minio/minio:latest` to `minio/minio:RELEASE.2025-04-22T22-12-26Z` for reproducibility (frozen Docker Hub images)
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt && cat infra/k8s/base/storage-class.yaml | grep -c "reclaimPolicy: Retain" && grep "local-path-retain" infra/k8s/base/postgres/statefulset.yaml && grep "local-path-retain" infra/k8s/base/minio/statefulset.yaml && grep "whenDeleted: Retain" infra/k8s/base/postgres/statefulset.yaml && grep "whenDeleted: Retain" infra/k8s/base/minio/statefulset.yaml && grep "RELEASE.2025-04-22" infra/k8s/base/minio/statefulset.yaml</automated>
  </verify>
  <acceptance_criteria>
    - infra/k8s/base/storage-class.yaml exists and contains `reclaimPolicy: Retain`
    - infra/k8s/base/storage-class.yaml contains `provisioner: rancher.io/local-path`
    - infra/k8s/base/storage-class.yaml contains `volumeBindingMode: WaitForFirstConsumer`
    - infra/k8s/base/storage-class.yaml does NOT contain `is-default-class`
    - infra/k8s/base/postgres/statefulset.yaml contains `storageClassName: local-path-retain`
    - infra/k8s/base/postgres/statefulset.yaml contains `whenDeleted: Retain`
    - infra/k8s/base/postgres/statefulset.yaml contains `whenScaled: Retain`
    - infra/k8s/base/minio/statefulset.yaml contains `storageClassName: local-path-retain`
    - infra/k8s/base/minio/statefulset.yaml contains `whenDeleted: Retain`
    - infra/k8s/base/minio/statefulset.yaml contains `RELEASE.2025-04-22T22-12-26Z`
  </acceptance_criteria>
  <done>Custom StorageClass created, PostgreSQL and MinIO StatefulSets updated with local-path-retain storageClassName, persistentVolumeClaimRetentionPolicy, and MinIO image pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Redis from Deployment to StatefulSet with RDB persistence</name>
  <files>
    infra/k8s/base/redis/statefulset.yaml,
    infra/k8s/base/redis/service.yaml,
    infra/k8s/base/kustomization.yaml,
    infra/k8s/overlays/staging/kustomization.yaml
  </files>
  <read_first>
    infra/k8s/base/redis/deployment.yaml,
    infra/k8s/base/redis/service.yaml,
    infra/k8s/base/kustomization.yaml,
    infra/k8s/overlays/staging/kustomization.yaml,
    infra/k8s/overlays/prod/kustomization.yaml
  </read_first>
  <action>
1. DELETE `infra/k8s/base/redis/deployment.yaml` (replaced by statefulset.yaml).

2. Create `infra/k8s/base/redis/statefulset.yaml` with this content:
```yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  labels:
    app: redis
    app.kubernetes.io/part-of: jobhunt
spec:
  serviceName: redis
  replicas: 1
  selector:
    matchLabels:
      app: redis
  updateStrategy:
    type: RollingUpdate
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  template:
    metadata:
      labels:
        app: redis
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
        - name: redis
          image: redis:7-alpine
          command: ["redis-server"]
          args:
            - "--save"
            - "3600 1 300 100 60 10000"
            - "--dir"
            - "/data"
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 96Mi
              cpu: 250m
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: local-path-retain
        resources:
          requests:
            storage: 1Gi
```

Key details preserved from existing deployment.yaml:
- Same resource limits (64Mi/96Mi, 50m/250m)
- Same securityContext (runAsNonRoot, runAsUser: 999, fsGroup: 999)
- Same probes (redis-cli ping)
- Same labels (app: redis, app.kubernetes.io/part-of: jobhunt)
- automountServiceAccountToken: false carried over

New additions:
- serviceName: redis (required for StatefulSet)
- command/args for RDB persistence with save policy
- volumeClaimTemplates with 1Gi local-path-retain PVC
- volumeMounts for /data
- persistentVolumeClaimRetentionPolicy: Retain/Retain
- updateStrategy: RollingUpdate

3. Modify `infra/k8s/base/redis/service.yaml` to make it headless:
Replace `type: ClusterIP` with `clusterIP: None`. Full content:
```yaml
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
```

4. Modify `infra/k8s/base/kustomization.yaml`:
- Add `storage-class.yaml` as first resource entry
- Change `redis/deployment.yaml` to `redis/statefulset.yaml`
Full resources list:
```yaml
resources:
  - storage-class.yaml
  - backend/deployment.yaml
  - backend/service.yaml
  - frontend/deployment.yaml
  - frontend/service.yaml
  - postgres/statefulset.yaml
  - postgres/service.yaml
  - redis/statefulset.yaml
  - redis/service.yaml
  - minio/statefulset.yaml
  - minio/service.yaml
```

5. Verify staging and prod overlay kustomization.yaml files:
- The `replicas` entries reference resources by `name: redis` which works for both Deployment and StatefulSet. No changes needed to staging or prod overlays since they already have `name: redis` replicas entries.
- Confirm by running kustomize build for both overlays.
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt && test ! -f infra/k8s/base/redis/deployment.yaml && test -f infra/k8s/base/redis/statefulset.yaml && grep "kind: StatefulSet" infra/k8s/base/redis/statefulset.yaml && grep "clusterIP: None" infra/k8s/base/redis/service.yaml && grep "storage-class.yaml" infra/k8s/base/kustomization.yaml && grep "redis/statefulset.yaml" infra/k8s/base/kustomization.yaml && kubectl kustomize infra/k8s/overlays/prod > /dev/null && kubectl kustomize infra/k8s/overlays/staging > /dev/null && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - infra/k8s/base/redis/deployment.yaml does NOT exist (deleted)
    - infra/k8s/base/redis/statefulset.yaml exists and contains `kind: StatefulSet`
    - infra/k8s/base/redis/statefulset.yaml contains `serviceName: redis`
    - infra/k8s/base/redis/statefulset.yaml contains `--save` and `3600 1 300 100 60 10000`
    - infra/k8s/base/redis/statefulset.yaml contains `--dir` and `/data`
    - infra/k8s/base/redis/statefulset.yaml contains `storageClassName: local-path-retain`
    - infra/k8s/base/redis/statefulset.yaml contains `storage: 1Gi`
    - infra/k8s/base/redis/statefulset.yaml contains `whenDeleted: Retain`
    - infra/k8s/base/redis/service.yaml contains `clusterIP: None`
    - infra/k8s/base/redis/service.yaml does NOT contain `type: ClusterIP`
    - infra/k8s/base/kustomization.yaml contains `storage-class.yaml`
    - infra/k8s/base/kustomization.yaml contains `redis/statefulset.yaml`
    - infra/k8s/base/kustomization.yaml does NOT contain `redis/deployment.yaml`
    - `kubectl kustomize infra/k8s/overlays/prod` exits 0
    - `kubectl kustomize infra/k8s/overlays/staging` exits 0
  </acceptance_criteria>
  <done>Redis converted from Deployment to StatefulSet with RDB persistence, headless service, 1Gi PVC. Base kustomization updated with StorageClass and Redis StatefulSet references. Both overlays build cleanly.</done>
</task>

</tasks>

<verification>
- `kubectl kustomize infra/k8s/overlays/prod` renders valid YAML without errors
- `kubectl kustomize infra/k8s/overlays/staging` renders valid YAML without errors
- `kubectl kustomize infra/k8s/overlays/prod | grep -c "local-path-retain"` returns 3 (postgres, redis, minio)
- `kubectl kustomize infra/k8s/overlays/prod | grep "kind: StorageClass"` shows the StorageClass
- Redis in prod output is StatefulSet (not Deployment)
- Staging replicas for redis remain 0
</verification>

<success_criteria>
- Custom StorageClass `local-path-retain` with reclaimPolicy Retain exists in base
- All three data store PVC templates reference `storageClassName: local-path-retain`
- All three StatefulSets have `persistentVolumeClaimRetentionPolicy: { whenDeleted: Retain, whenScaled: Retain }`
- Redis is a StatefulSet with RDB persistence (`--save`, `--dir /data`, 1Gi PVC)
- Redis service is headless (`clusterIP: None`)
- MinIO image pinned to specific release tag
- Kustomize build succeeds for both overlays
</success_criteria>

<output>
After completion, create `.planning/phases/16-data-stores-on-k8s/16-01-SUMMARY.md`
</output>
