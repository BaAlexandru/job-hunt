---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - CLAUDE.md
  - backend/CLAUDE.md
  - frontend/CLAUDE.md
  - infra/CLAUDE.md
  - .claude/skills/SKILL.md
  - .claude/skills/rules/project-conventions.md
autonomous: true
requirements:
  - DEVX-01
  - DEVX-02

must_haves:
  truths:
    - "CLAUDE.md exists at project root with monorepo-wide guidance"
    - "CLAUDE.md exists in /backend with Spring Boot + Kotlin conventions"
    - "CLAUDE.md exists in /frontend with Next.js placeholder guidance"
    - "CLAUDE.md exists in /infra with Docker/infrastructure guidance"
    - ".claude/skills/ directory contains project skill definitions"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Root-level Claude instructions for monorepo"
      contains: "backend"
    - path: "backend/CLAUDE.md"
      provides: "Backend-specific Kotlin/Spring Boot conventions"
      contains: "Spring Boot"
    - path: "frontend/CLAUDE.md"
      provides: "Frontend placeholder instructions"
      contains: "Next.js"
    - path: "infra/CLAUDE.md"
      provides: "Infrastructure/Docker instructions"
      contains: "Docker"
    - path: ".claude/skills/SKILL.md"
      provides: "Skill index for Claude Code"
      contains: "rules"
  key_links:
    - from: "CLAUDE.md"
      to: "backend/CLAUDE.md"
      via: "Reference to module-specific files"
      pattern: "backend/CLAUDE.md"
---

<objective>
Create CLAUDE.md files for each module directory and set up the .claude/skills/ directory with project-level Claude Code skills.

Purpose: Developer experience tooling ensures Claude Code agents working on any part of the monorepo have module-specific context, conventions, and rules available without needing to explore the codebase.
Output: CLAUDE.md in root, backend, frontend, infra + .claude/skills/ with project conventions.
</objective>

<execution_context>
@C:/Users/balab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/balab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md files for root and all module directories</name>
  <files>
    CLAUDE.md,
    backend/CLAUDE.md,
    frontend/CLAUDE.md,
    infra/CLAUDE.md
  </files>
  <read_first>
    backend/build.gradle.kts,
    backend/src/main/resources/application.yml,
    compose.yaml,
    settings.gradle.kts
  </read_first>
  <action>
    Create four CLAUDE.md files with module-specific guidance. Each file should be concise and actionable (not documentation for documentation's sake).

    1. CREATE CLAUDE.md (project root):
    ```markdown
    # JobHunt - Claude Code Instructions

    ## Project Structure

    Gradle multi-project monorepo:
    - /backend - Spring Boot 4.0.4 + Kotlin 2.2.21 REST API
    - /frontend - Next.js (not yet initialized, Phase 7)
    - /infra - Docker and deployment config
    - compose.yaml at root for Spring Boot docker-compose auto-discovery

    ## Key Commands

    - Build: `./gradlew :backend:classes`
    - Test: `./gradlew :backend:test`
    - Run: `./gradlew :backend:bootRun` (auto-starts PostgreSQL via Docker Compose)
    - Format check: `./gradlew :backend:check`

    ## Conventions

    - Kotlin data classes, never Lombok
    - UUID primary keys via gen_random_uuid() for all tables
    - Flyway migrations: V{N}__{phaseNN}_{description}.sql
    - JPA hibernate.ddl-auto=validate (Flyway manages schema)
    - No database connection properties in application.yml (docker-compose auto-configures)
    - Dependencies added only when their phase starts (no preloading)

    ## Module-Specific Instructions

    See each module's CLAUDE.md for detailed conventions:
    - backend/CLAUDE.md - Kotlin, Spring Boot, JPA, API patterns
    - frontend/CLAUDE.md - Next.js, React (Phase 7+)
    - infra/CLAUDE.md - Docker, deployment
    ```

    2. CREATE backend/CLAUDE.md:
    ```markdown
    # Backend - Claude Code Instructions

    ## Stack

    - Kotlin 2.2.21 with Java 24 toolchain
    - Spring Boot 4.0.4 (uses renamed starters: webmvc, not web)
    - Spring Data JPA with PostgreSQL
    - Flyway for database migrations
    - JUnit 5 + Spring Boot Test

    ## Package Structure

    Base package: com.alex.job.hunt.jobhunt
    - /controller - REST controllers (@RestController)
    - /service - Business logic (@Service)
    - /repository - Data access (@Repository, extends JpaRepository)
    - /entity - JPA entities (@Entity)
    - /dto - Request/response data classes

    ## Coding Conventions

    - Use Kotlin idioms: data classes, extension functions, null safety, when expressions
    - Entity classes: use @Entity annotation (allOpen plugin handles making them open)
    - No Lombok -- use Kotlin language features instead
    - API endpoints under /api/ prefix (e.g., /api/health, /api/users)
    - Return data classes from controllers (Jackson Kotlin module handles serialization)
    - Use constructor injection (Kotlin primary constructors), never @Autowired

    ## Database

    - Flyway manages ALL schema changes, never hibernate ddl-auto
    - Migration naming: V{N}__{phaseNN}_{description}.sql
    - UUID primary keys: use gen_random_uuid() as default in SQL
    - All tables should have created_at and updated_at timestamp columns

    ## Testing

    - @SpringBootTest for integration tests (loads full context, starts Docker Compose)
    - @WebMvcTest for controller-only tests (no database)
    - Test command: ../gradlew test (from backend/) or ./gradlew :backend:test (from root)

    ## Spring Boot 4.0 Specifics

    - Starters renamed: use spring-boot-starter-webmvc (not starter-web)
    - Jackson group: tools.jackson.module (not com.fasterxml.jackson)
    - Flyway: use spring-boot-starter-flyway (not raw flyway-core)
    - Modular starters: each feature needs its own starter dependency
    ```

    3. CREATE frontend/CLAUDE.md:
    ```markdown
    # Frontend - Claude Code Instructions

    ## Status

    Not yet initialized. Will be set up in Phase 7.

    ## Planned Stack

    - Next.js with TypeScript
    - pnpm package manager
    - Not a Gradle subproject -- standalone Node.js project in this directory

    ## When Implementing (Phase 7+)

    - API base URL: http://localhost:8080/api
    - Auth: JWT tokens from backend /api/auth endpoints
    - All API calls go through a centralized API client
    ```

    4. CREATE infra/CLAUDE.md:
    ```markdown
    # Infrastructure - Claude Code Instructions

    ## Current Setup

    - compose.yaml at project root (not in this directory) for Spring Boot auto-discovery
    - PostgreSQL 17 container for local development
    - Backend runs on host via Gradle (not containerized for dev)

    ## Directory Purpose

    - /docker - Dockerfiles for production builds (future phases)
    - No Kubernetes/Helm configs (out of scope for v1)

    ## Docker Compose

    - compose.yaml lives at project root, NOT in /infra
    - Spring Boot docker-compose starter auto-discovers it there
    - Fixed port mapping: 5432:5432
    - Named volume: pgdata for data persistence
    ```
  </action>
  <verify>
    <automated>ls D:/dev/projects/job-hunt/CLAUDE.md D:/dev/projects/job-hunt/backend/CLAUDE.md D:/dev/projects/job-hunt/frontend/CLAUDE.md D:/dev/projects/job-hunt/infra/CLAUDE.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md (root) contains `./gradlew :backend:bootRun` and `backend/CLAUDE.md`
    - backend/CLAUDE.md contains `Spring Boot 4.0.4` and `Kotlin 2.2.21` and `spring-boot-starter-webmvc` and `gen_random_uuid()`
    - frontend/CLAUDE.md contains `Next.js` and `Phase 7`
    - infra/CLAUDE.md contains `Docker` and `compose.yaml at project root`
    - All four files exist and are non-empty
  </acceptance_criteria>
  <done>CLAUDE.md files exist in root, backend, frontend, and infra with accurate module-specific guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Create .claude/skills/ directory with project conventions skill</name>
  <files>
    .claude/skills/SKILL.md,
    .claude/skills/rules/project-conventions.md
  </files>
  <read_first>
    CLAUDE.md,
    backend/CLAUDE.md
  </read_first>
  <action>
    Set up the .claude/skills/ directory structure with a project conventions skill.

    1. CREATE .claude/skills/SKILL.md:
    ```markdown
    # JobHunt - Claude Code Skills

    ## Available Skills

    ### project-conventions
    Project-wide coding conventions, patterns, and rules.
    - Rules: rules/project-conventions.md

    ## Usage

    These skills are auto-loaded by Claude Code agents working on this project.
    Each skill has a SKILL.md index and detailed rules in the rules/ subdirectory.
    ```

    2. CREATE .claude/skills/rules/project-conventions.md:
    ```markdown
    # Project Conventions

    ## Architecture Rules

    1. Backend-first development: API built and tested before frontend
    2. Gradle multi-project: plugins declared in root with `apply false`, applied in subprojects without versions
    3. No `subprojects {}` block in root build.gradle.kts
    4. Each phase adds only the dependencies it needs

    ## Kotlin Rules

    1. Use data classes for DTOs and value objects
    2. Use constructor injection via primary constructors, never @Autowired fields
    3. Use Kotlin null safety instead of Optional<T>
    4. Use `when` expressions instead of if-else chains for type/enum matching
    5. No Lombok -- Kotlin language features replace it entirely

    ## Database Rules

    1. Flyway manages ALL schema changes
    2. hibernate.ddl-auto must be `validate`, never `update` or `create-drop`
    3. Migration naming: V{N}__{phaseNN}_{description}.sql
    4. UUID primary keys with gen_random_uuid() default
    5. All tables include created_at TIMESTAMPTZ and updated_at TIMESTAMPTZ columns
    6. Multi-user ready: all domain tables have a user_id foreign key

    ## API Rules

    1. All endpoints under /api/ prefix
    2. RESTful resource naming: /api/companies, /api/jobs, /api/applications
    3. Return appropriate HTTP status codes: 201 for create, 204 for delete, 400 for validation, 404 for not found
    4. Use data classes for request/response bodies

    ## Testing Rules

    1. @SpringBootTest for integration tests
    2. @WebMvcTest(Controller::class) for controller unit tests
    3. Test files mirror source structure in src/test/kotlin/
    4. Test class naming: {ClassName}Tests.kt

    ## Docker Rules

    1. compose.yaml at project root (not in /infra) for Spring Boot auto-discovery
    2. No database connection properties in application.yml (docker-compose auto-configures)
    3. Fixed port mappings, not random
    4. Named volumes for data persistence
    ```
  </action>
  <verify>
    <automated>ls D:/dev/projects/job-hunt/.claude/skills/SKILL.md D:/dev/projects/job-hunt/.claude/skills/rules/project-conventions.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/SKILL.md exists and contains `project-conventions`
    - .claude/skills/rules/project-conventions.md exists and contains `Flyway` and `gen_random_uuid` and `ddl-auto` and `validate`
    - .claude/skills/rules/project-conventions.md contains `constructor injection` and `No Lombok`
  </acceptance_criteria>
  <done>.claude/skills/ directory exists with SKILL.md index and project-conventions.md rules file.</done>
</task>

</tasks>

<verification>
1. All four CLAUDE.md files exist and contain module-appropriate content
2. .claude/skills/SKILL.md exists with skill index
3. .claude/skills/rules/project-conventions.md exists with full conventions
4. `./gradlew :backend:test` still passes (no regressions from doc-only changes)
</verification>

<success_criteria>
- CLAUDE.md files present in: root, backend/, frontend/, infra/
- Each CLAUDE.md contains accurate, module-specific guidance
- .claude/skills/SKILL.md indexes available skills
- .claude/skills/rules/project-conventions.md documents Kotlin, DB, API, and testing conventions
- No build regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md`
</output>
