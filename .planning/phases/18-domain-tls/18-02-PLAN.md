---
phase: 18-domain-tls
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - infra/k8s/traefik/traefik-config.yaml
  - infra/k8s/traefik/tls-store.yaml
  - infra/k8s/traefik/tls-option.yaml
  - infra/k8s/argocd/ingress-route-tcp.yaml
  - infra/scripts/change-argocd-password.sh
autonomous: true
requirements: [DNS-05]

must_haves:
  truths:
    - "Traefik redirects all HTTP traffic to HTTPS via HelmChartConfig"
    - "Traefik uses Origin CA cert as default TLS certificate via TLSStore"
    - "Traefik enforces TLS 1.2 minimum via TLSOption"
    - "Traefik trusts Cloudflare IPv4 ranges for X-Forwarded-For headers"
    - "ArgoCD is accessible at argocd.job-hunt.dev via TLS passthrough"
    - "ArgoCD admin password persists (no Application manages argocd-secret)"
  artifacts:
    - path: "infra/k8s/traefik/traefik-config.yaml"
      provides: "HelmChartConfig for HTTP redirect + Cloudflare trustedIPs"
      contains: "HelmChartConfig"
    - path: "infra/k8s/traefik/tls-store.yaml"
      provides: "TLSStore default cert pointing to origin-ca-tls secret"
      contains: "TLSStore"
    - path: "infra/k8s/traefik/tls-option.yaml"
      provides: "TLSOption enforcing TLS 1.2 minimum"
      contains: "TLSOption"
    - path: "infra/k8s/argocd/ingress-route-tcp.yaml"
      provides: "IngressRouteTCP for ArgoCD TLS passthrough"
      contains: "IngressRouteTCP"
    - path: "infra/scripts/change-argocd-password.sh"
      provides: "Helper script for ArgoCD admin password change"
      contains: "bcrypt"
  key_links:
    - from: "infra/k8s/traefik/tls-store.yaml"
      to: "origin-ca-tls Secret in kube-system"
      via: "secretName reference"
      pattern: "secretName.*origin-ca-tls"
    - from: "infra/k8s/argocd/ingress-route-tcp.yaml"
      to: "argocd-server Service"
      via: "HostSNI match + service reference"
      pattern: "HostSNI.*argocd\\.job-hunt\\.dev"
    - from: "infra/scripts/change-argocd-password.sh"
      to: "argocd-secret in argocd namespace"
      via: "kubectl patch to update admin password"
      pattern: "kubectl.*patch.*argocd-secret"
---

<objective>
Create Traefik TLS configuration (HelmChartConfig, TLSStore, TLSOption), ArgoCD IngressRouteTCP for TLS passthrough at argocd.job-hunt.dev, and the ArgoCD admin password change script.

Purpose: These K8s manifests configure the origin-side TLS chain. Traefik gets its default cert (from Plan 01's sealed secret), enforces HTTPS redirect and TLS 1.2+, and routes ArgoCD traffic via TLS passthrough. ArgoCD password persists because argocd-secret is Helm-managed, not managed by app-of-apps.

Output: Traefik CRD manifests, ArgoCD IngressRouteTCP, updated ArgoCD Application specs, password change script
</objective>

<execution_context>
@C:/Users/balab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/balab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-domain-tls/18-CONTEXT.md
@.planning/phases/18-domain-tls/18-RESEARCH.md

<interfaces>
<!-- ArgoCD Application specs — NOT modified (argocd-secret not managed by these apps) -->

From infra/argocd/apps/prod.yaml:
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobhunt-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:baalexandru/job-hunt.git
    targetRevision: master
    path: infra/k8s/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: jobhunt-prod
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
```

From infra/argocd/apps/staging.yaml:
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobhunt-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:baalexandru/job-hunt.git
    targetRevision: master
    path: infra/k8s/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: jobhunt-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
```

From infra/k8s/overlays/prod/ingress.yaml:
```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jobhunt-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  rules:
    - host: job-hunt.dev
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
```
<!-- NOTE: No spec.tls section needed -- TLSStore default cert handles TLS automatically -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Traefik TLS configuration manifests</name>
  <files>
    infra/k8s/traefik/traefik-config.yaml,
    infra/k8s/traefik/tls-store.yaml,
    infra/k8s/traefik/tls-option.yaml
  </files>
  <read_first>
    .planning/phases/18-domain-tls/18-RESEARCH.md,
    .planning/phases/18-domain-tls/18-CONTEXT.md,
    infra/k8s/overlays/prod/ingress.yaml,
    infra/k8s/overlays/staging/ingress.yaml
  </read_first>
  <action>
Create the `infra/k8s/traefik/` directory and three manifest files.

**1. Create `infra/k8s/traefik/traefik-config.yaml`** -- K3s HelmChartConfig to customize bundled Traefik:

```yaml
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    ports:
      web:
        redirectTo:
          port: websecure
        forwardedHeaders:
          trustedIPs:
            - 173.245.48.0/20
            - 103.21.244.0/22
            - 103.22.200.0/22
            - 103.31.4.0/22
            - 141.101.64.0/18
            - 108.162.192.0/18
            - 190.93.240.0/20
            - 188.114.96.0/20
            - 197.234.240.0/22
            - 198.41.128.0/17
            - 162.158.0.0/15
            - 104.16.0.0/13
            - 104.24.0.0/14
            - 172.64.0.0/13
            - 131.0.72.0/22
      websecure:
        tls:
          enabled: true
        forwardedHeaders:
          trustedIPs:
            - 173.245.48.0/20
            - 103.21.244.0/22
            - 103.22.200.0/22
            - 103.31.4.0/22
            - 141.101.64.0/18
            - 108.162.192.0/18
            - 190.93.240.0/20
            - 188.114.96.0/20
            - 197.234.240.0/22
            - 198.41.128.0/17
            - 162.158.0.0/15
            - 104.16.0.0/13
            - 104.24.0.0/14
            - 172.64.0.0/13
            - 131.0.72.0/22
```

This enables TLS on the `websecure` entrypoint, redirects HTTP (port `web`) to HTTPS (port `websecure`), and trusts Cloudflare IPv4 ranges on BOTH entrypoints for `X-Forwarded-For` headers so application logs show real client IPs. Uses proper Helm values structure (not `additionalArguments`) per traefik-k3s skill reference.

**2. Create `infra/k8s/traefik/tls-store.yaml`** -- Default TLS certificate for all Ingress resources:

```yaml
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: kube-system
spec:
  defaultCertificate:
    secretName: origin-ca-tls
```

The name `default` is special -- Traefik uses it automatically for all TLS connections. The `origin-ca-tls` Secret is the SealedSecret created by `seal-origin-ca.sh` in kube-system namespace.

**3. Create `infra/k8s/traefik/tls-option.yaml`** -- Enforce minimum TLS 1.2 on origin:

```yaml
apiVersion: traefik.io/v1alpha1
kind: TLSOption
metadata:
  name: default
  namespace: kube-system
spec:
  minVersion: VersionTLS12
```

Like TLSStore, the `default` name is automatically applied to all TLS connections.

**Important:** Do NOT modify the existing Ingress resources in overlays/prod/ or overlays/staging/. They do NOT need `spec.tls` sections because TLSStore handles TLS automatically.
  </action>
  <verify>
    <automated>ls infra/k8s/traefik/traefik-config.yaml infra/k8s/traefik/tls-store.yaml infra/k8s/traefik/tls-option.yaml && echo "All 3 Traefik manifest files exist"</automated>
  </verify>
  <acceptance_criteria>
    - traefik-config.yaml has `apiVersion: helm.cattle.io/v1` and `kind: HelmChartConfig`
    - traefik-config.yaml has `name: traefik` in metadata and `namespace: kube-system`
    - traefik-config.yaml contains `redirectTo:` with `port: websecure`
    - traefik-config.yaml contains `forwardedHeaders:` + `trustedIPs:` as YAML list (not additionalArguments) on BOTH `web` and `websecure` entrypoints
    - traefik-config.yaml contains `173.245.48.0/20` (first CIDR) and `131.0.72.0/22` (last CIDR)
    - tls-store.yaml has `apiVersion: traefik.io/v1alpha1` and `kind: TLSStore`
    - tls-store.yaml has `name: default` and `namespace: kube-system`
    - tls-store.yaml has `secretName: origin-ca-tls`
    - tls-option.yaml has `apiVersion: traefik.io/v1alpha1` and `kind: TLSOption`
    - tls-option.yaml has `name: default` and `namespace: kube-system`
    - tls-option.yaml has `minVersion: VersionTLS12`
    - None of the overlay ingress.yaml files are modified (no spec.tls added)
  </acceptance_criteria>
  <done>Three Traefik configuration manifests created in infra/k8s/traefik/. HelmChartConfig enables HTTPS redirect and Cloudflare trusted IPs. TLSStore sets default cert. TLSOption enforces TLS 1.2+. Existing Ingress resources are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ArgoCD TLS passthrough and password script</name>
  <files>
    infra/k8s/argocd/ingress-route-tcp.yaml,
    infra/scripts/change-argocd-password.sh
  </files>
  <read_first>
    infra/argocd/values.yaml,
    .planning/phases/18-domain-tls/18-RESEARCH.md,
    .planning/phases/18-domain-tls/18-CONTEXT.md
  </read_first>
  <action>
**1. Create `infra/k8s/argocd/` directory and `infra/k8s/argocd/ingress-route-tcp.yaml`** -- ArgoCD TLS passthrough:

```yaml
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`argocd.job-hunt.dev`)
      services:
        - name: argocd-server
          port: 443
  tls:
    passthrough: true
```

This uses TLS passthrough so ArgoCD handles its own TLS termination with the Origin CA cert (via argocd-server-tls Secret). The `HostSNI` match ensures only argocd.job-hunt.dev traffic goes through passthrough; other hosts use standard Traefik TLS termination.

**2. ArgoCD Application specs — NO changes needed.**

The `argocd-secret` (containing admin password) is NOT managed by any ArgoCD Application. It's part of the ArgoCD Helm installation in the `argocd` namespace. The `jobhunt-prod` and `jobhunt-staging` Applications only manage resources in `jobhunt-prod`/`jobhunt-staging` namespaces respectively. Since no Application manages `argocd-secret`, there's nothing to revert the password — `ignoreDifferences` is unnecessary.

`infra/argocd/apps/prod.yaml` and `infra/argocd/apps/staging.yaml` remain unchanged.

**4. Create `infra/scripts/change-argocd-password.sh`** -- Admin password change helper:

```bash
#!/usr/bin/env bash
# change-argocd-password.sh -- Change ArgoCD admin password
# Usage: ./change-argocd-password.sh
# Prompts for new password, hashes with bcrypt, patches argocd-secret
set -euo pipefail

echo "Enter new ArgoCD admin password:"
read -rs NEW_PASSWORD
echo ""

if [ -z "$NEW_PASSWORD" ]; then
  echo "ERROR: Password cannot be empty"
  exit 1
fi

echo "Generating bcrypt hash..."
# Use python3 bcrypt (preferred) or htpasswd fallback
# NOTE: Password passed via env var to avoid shell injection (never interpolate into code strings)
if command -v python3 &> /dev/null && python3 -c "import bcrypt" 2>/dev/null; then
  BCRYPT_HASH=$(NEW_PASSWORD="$NEW_PASSWORD" python3 -c "
import bcrypt, os
password = os.environ['NEW_PASSWORD'].encode('utf-8')
print(bcrypt.hashpw(password, bcrypt.gensalt(rounds=10)).decode('utf-8'))
")
elif command -v htpasswd &> /dev/null; then
  BCRYPT_HASH=$(htpasswd -nbBC 10 "" "$NEW_PASSWORD" | tr -d ':\n' | sed 's/$2y/$2a/')
else
  echo "ERROR: Requires python3 with bcrypt module OR htpasswd"
  echo "Install: pip3 install bcrypt OR apt-get install apache2-utils"
  exit 1
fi

MTIME=$(date -u +%FT%TZ)

echo "Patching argocd-secret..."
kubectl -n argocd patch secret argocd-secret \
  -p "{\"stringData\": {\"admin.password\": \"${BCRYPT_HASH}\", \"admin.passwordMtime\": \"${MTIME}\"}}"

echo "Restarting argocd-server to pick up change..."
kubectl -n argocd rollout restart deployment argocd-server
kubectl -n argocd rollout status deployment argocd-server --timeout=120s

echo ""
echo "ArgoCD admin password changed successfully."
echo "IMPORTANT: Store the password in your password manager."
echo "Login: https://argocd.job-hunt.dev (username: admin)"
```

Make executable: `chmod +x infra/scripts/change-argocd-password.sh`
  </action>
  <verify>
    <automated>bash -n infra/scripts/change-argocd-password.sh && ls infra/k8s/argocd/ingress-route-tcp.yaml && echo "All files valid"</automated>
  </verify>
  <acceptance_criteria>
    - ingress-route-tcp.yaml has `apiVersion: traefik.io/v1alpha1` and `kind: IngressRouteTCP`
    - ingress-route-tcp.yaml has `namespace: argocd`
    - ingress-route-tcp.yaml contains `HostSNI(`argocd.job-hunt.dev`)`
    - ingress-route-tcp.yaml contains `passthrough: true`
    - ingress-route-tcp.yaml references `name: argocd-server` service on `port: 443`
    - prod.yaml and staging.yaml are NOT modified (argocd-secret not managed by these Applications)
    - change-argocd-password.sh contains `read -rs NEW_PASSWORD` (secure password input)
    - change-argocd-password.sh passes password to python3 via env var (not string interpolation — prevents injection)
    - change-argocd-password.sh contains `kubectl -n argocd patch secret argocd-secret`
    - change-argocd-password.sh contains `kubectl -n argocd rollout restart deployment argocd-server`
    - change-argocd-password.sh passes `bash -n` syntax check
    - change-argocd-password.sh is executable (chmod +x)
  </acceptance_criteria>
  <done>ArgoCD IngressRouteTCP configured for TLS passthrough at argocd.job-hunt.dev. ArgoCD Application specs unchanged (argocd-secret not managed by app-of-apps, no ignoreDifferences needed). Password change script created with secure env-var-based bcrypt hashing and htpasswd fallback.</done>
</task>

</tasks>

<verification>
- All 3 Traefik manifests exist in infra/k8s/traefik/ with correct API versions (traefik.io/v1alpha1 for CRDs, helm.cattle.io/v1 for HelmChartConfig)
- HelmChartConfig uses Helm values structure for trustedIPs on BOTH web and websecure entrypoints (not additionalArguments)
- ArgoCD IngressRouteTCP uses TLS passthrough on websecure entrypoint
- ArgoCD Application specs are NOT modified (argocd-secret not managed by app-of-apps)
- change-argocd-password.sh passes bash syntax check, uses env var for password (no injection)
- No existing Ingress resources were modified
</verification>

<success_criteria>
All K8s manifests are created with correct structure. ArgoCD is configured for TLS passthrough exposure. Password change protection is in place. The origin TLS chain is fully defined: Traefik default cert (TLSStore) + minimum TLS version (TLSOption) + HTTP redirect (HelmChartConfig) + ArgoCD passthrough (IngressRouteTCP).
</success_criteria>

<output>
After completion, create `.planning/phases/18-domain-tls/18-02-SUMMARY.md`
</output>
