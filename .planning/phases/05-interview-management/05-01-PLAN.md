---
phase: 05-interview-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V11__phase05_create_interviews.sql
  - backend/src/main/resources/db/migration/V12__phase05_create_interview_notes.sql
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/enums.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewEntity.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewNoteEntity.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/InterviewRepository.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/InterviewNoteRepository.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/InterviewDtos.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/InterviewNoteDtos.kt
  - backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/TimelineDtos.kt
autonomous: true
requirements: [INTV-01, INTV-02, INTV-03, INTV-04]

must_haves:
  truths:
    - "Interview and interview_notes tables exist in PostgreSQL with correct schema"
    - "All 5 new enums (InterviewType, InterviewStage, InterviewOutcome, InterviewResult, InterviewNoteType) are available in Kotlin"
    - "JPA entities map correctly to DB tables (hibernate validate passes)"
    - "Repositories provide all query methods needed by service layer"
    - "DTOs define request/response contracts for interviews, interview notes, and timeline"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V11__phase05_create_interviews.sql"
      provides: "interviews table with indexes"
      contains: "CREATE TABLE interviews"
    - path: "backend/src/main/resources/db/migration/V12__phase05_create_interview_notes.sql"
      provides: "interview_notes table with indexes"
      contains: "CREATE TABLE interview_notes"
    - path: "backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/enums.kt"
      provides: "All interview-related enums"
      contains: "enum class InterviewType"
    - path: "backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewEntity.kt"
      provides: "Interview JPA entity"
      contains: "class InterviewEntity"
    - path: "backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewNoteEntity.kt"
      provides: "InterviewNote JPA entity"
      contains: "class InterviewNoteEntity"
    - path: "backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/InterviewDtos.kt"
      provides: "Interview request/response DTOs"
      exports: ["CreateInterviewRequest", "UpdateInterviewRequest", "InterviewResponse"]
    - path: "backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/TimelineDtos.kt"
      provides: "Timeline entry DTOs"
      exports: ["TimelineEntryType", "TimelineEntry"]
  key_links:
    - from: "V11__phase05_create_interviews.sql"
      to: "InterviewEntity.kt"
      via: "hibernate validate"
      pattern: "@Table\\(name = \"interviews\"\\)"
    - from: "V12__phase05_create_interview_notes.sql"
      to: "InterviewNoteEntity.kt"
      via: "hibernate validate"
      pattern: "@Table\\(name = \"interview_notes\"\\)"
---

<objective>
Create the data layer for interview management: Flyway migrations, JPA entities, repositories, and DTO contracts.

Purpose: Establish the foundation (schema, entities, DTOs) that Plan 02 builds API endpoints and tests on top of.
Output: 2 Flyway migrations, 2 JPA entities, 5 new enums, 2 repositories, 3 DTO files.
</objective>

<execution_context>
@C:/Users/balab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/balab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-interview-management/05-CONTEXT.md
@.planning/phases/05-interview-management/05-RESEARCH.md

@backend/CLAUDE.md
@backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/enums.kt
@backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/ApplicationNoteEntity.kt
@backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/ApplicationEntity.kt
@backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/ApplicationNoteRepository.kt
@backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/ApplicationNoteDtos.kt

<interfaces>
<!-- Existing patterns to replicate exactly -->

From backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/enums.kt:
```kotlin
enum class WorkMode { ONSITE, REMOTE, HYBRID }
enum class JobType { FULL_TIME, PART_TIME, CONTRACT, FREELANCE, INTERNSHIP }
enum class SalaryType { RANGE, FIXED, TEXT }
enum class SalaryPeriod { ANNUAL, MONTHLY, HOURLY, DAILY }
enum class ApplicationStatus { INTERESTED, APPLIED, PHONE_SCREEN, INTERVIEW, OFFER, REJECTED, ACCEPTED, WITHDRAWN }
enum class NoteType { GENERAL, PHONE_CALL, EMAIL, FOLLOW_UP, STATUS_CHANGE }
```

From backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/ApplicationNoteEntity.kt:
```kotlin
@Entity
@Table(name = "application_notes")
class ApplicationNoteEntity(
    @Id @GeneratedValue(strategy = GenerationType.UUID) @Column(updatable = false, nullable = false) val id: UUID? = null,
    @Column(name = "application_id", nullable = false) val applicationId: UUID,
    @Column(nullable = false, columnDefinition = "TEXT") var content: String,
    @Column(name = "note_type", nullable = false, length = 50) @Enumerated(EnumType.STRING) var noteType: NoteType = NoteType.GENERAL,
    @Column(name = "created_at", updatable = false, nullable = false) val createdAt: Instant = Instant.now(),
    @Column(name = "updated_at", nullable = false) var updatedAt: Instant = Instant.now()
)
```

From backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/ApplicationNoteRepository.kt:
```kotlin
interface ApplicationNoteRepository : JpaRepository<ApplicationNoteEntity, UUID> {
    fun findByIdAndApplicationId(id: UUID, applicationId: UUID): ApplicationNoteEntity?
    fun findByApplicationIdOrderByCreatedAtDesc(applicationId: UUID, pageable: Pageable): Page<ApplicationNoteEntity>
    fun findByApplicationIdOrderByCreatedAtDesc(applicationId: UUID): List<ApplicationNoteEntity>
}
```

From backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/ApplicationNoteDtos.kt:
```kotlin
data class CreateNoteRequest(
    @field:NotBlank(message = "Content is required") @field:Size(max = 10000) val content: String,
    val noteType: NoteType? = null
)
data class NoteResponse(val id: UUID, val applicationId: UUID, val content: String, val noteType: NoteType, val createdAt: Instant, val updatedAt: Instant)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway migrations, enums, and JPA entities</name>
  <files>
    backend/src/main/resources/db/migration/V11__phase05_create_interviews.sql,
    backend/src/main/resources/db/migration/V12__phase05_create_interview_notes.sql,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/enums.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewEntity.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewNoteEntity.kt
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V7__phase04_create_applications.sql,
    backend/src/main/resources/db/migration/V8__phase04_create_application_notes.sql,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/enums.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/ApplicationNoteEntity.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/ApplicationEntity.kt
  </read_first>
  <action>
**1. Create V11__phase05_create_interviews.sql:**
```sql
CREATE TABLE interviews (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    application_id      UUID         NOT NULL REFERENCES applications(id) ON DELETE CASCADE,
    user_id             UUID         NOT NULL REFERENCES users(id),
    round_number        INT          NOT NULL,
    scheduled_at        TIMESTAMPTZ  NOT NULL,
    duration_minutes    INT          DEFAULT 60,
    interview_type      VARCHAR(50)  NOT NULL,
    stage               VARCHAR(50)  NOT NULL,
    stage_label         VARCHAR(255),
    outcome             VARCHAR(50)  NOT NULL DEFAULT 'SCHEDULED',
    result              VARCHAR(50)  NOT NULL DEFAULT 'PENDING',
    location            TEXT,
    interviewer_names   VARCHAR(500),
    candidate_feedback  TEXT,
    company_feedback    TEXT,
    archived            BOOLEAN      NOT NULL DEFAULT FALSE,
    archived_at         TIMESTAMPTZ,
    created_at          TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at          TIMESTAMPTZ  NOT NULL DEFAULT now()
);

CREATE INDEX idx_interviews_application_id ON interviews(application_id);
CREATE INDEX idx_interviews_user_id ON interviews(user_id);
CREATE INDEX idx_interviews_user_archived ON interviews(user_id, archived);
CREATE INDEX idx_interviews_scheduled_at ON interviews(application_id, scheduled_at);
CREATE INDEX idx_interviews_stage ON interviews(application_id, stage);
```

**2. Create V12__phase05_create_interview_notes.sql:**
```sql
CREATE TABLE interview_notes (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    interview_id    UUID         NOT NULL REFERENCES interviews(id) ON DELETE CASCADE,
    content         TEXT         NOT NULL,
    note_type       VARCHAR(50)  NOT NULL DEFAULT 'GENERAL',
    created_at      TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at      TIMESTAMPTZ  NOT NULL DEFAULT now()
);

CREATE INDEX idx_interview_notes_interview_id ON interview_notes(interview_id);
```

**3. Append 5 new enums to enums.kt** (keep all existing enums, add at the bottom):
```kotlin
enum class InterviewType { PHONE, VIDEO, ONSITE, TAKE_HOME }
enum class InterviewStage { SCREENING, TECHNICAL, BEHAVIORAL, CULTURE_FIT, FINAL, SYSTEM_DESIGN, HOMEWORK, OTHER }
enum class InterviewOutcome { SCHEDULED, COMPLETED, CANCELLED, NO_SHOW }
enum class InterviewResult { PASSED, FAILED, PENDING, MIXED }
enum class InterviewNoteType { PREPARATION, QUESTION_ASKED, FEEDBACK, FOLLOW_UP, GENERAL }
```

**4. Create InterviewEntity.kt** following the ApplicationEntity pattern:
- Package: `com.alex.job.hunt.jobhunt.entity`
- Class (not data class) with `@Entity`, `@Table(name = "interviews")`
- Fields: id (UUID? = null), applicationId (UUID), userId (UUID), roundNumber (Int), scheduledAt (Instant), durationMinutes (Int? = 60), interviewType (InterviewType), stage (InterviewStage), stageLabel (String? = null), outcome (InterviewOutcome = InterviewOutcome.SCHEDULED), result (InterviewResult = InterviewResult.PENDING), location (String? = null), interviewerNames (String? = null), candidateFeedback (String? = null), companyFeedback (String? = null), archived (Boolean = false), archivedAt (Instant? = null), createdAt (Instant = Instant.now()), updatedAt (Instant = Instant.now())
- All enums mapped with `@Enumerated(EnumType.STRING)` and `length = 50`
- All column names explicitly snake_case via `@Column(name = "...")`
- TEXT fields: location, candidateFeedback, companyFeedback use `columnDefinition = "TEXT"`
- interviewerNames: `@Column(name = "interviewer_names", length = 500)`
- equals/hashCode using id-based pattern (same as ApplicationNoteEntity)

**5. Create InterviewNoteEntity.kt** following ApplicationNoteEntity pattern exactly:
- Package: `com.alex.job.hunt.jobhunt.entity`
- Class with `@Entity`, `@Table(name = "interview_notes")`
- Fields: id (UUID? = null), interviewId (UUID), content (String, TEXT), noteType (InterviewNoteType = InterviewNoteType.GENERAL), createdAt (Instant), updatedAt (Instant)
- Note: NO userId field -- user isolation is through parent interview
- equals/hashCode using id-based pattern
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt && ./gradlew :backend:classes</automated>
  </verify>
  <acceptance_criteria>
    - V11__phase05_create_interviews.sql contains "CREATE TABLE interviews" with "application_id UUID NOT NULL REFERENCES applications(id)"
    - V12__phase05_create_interview_notes.sql contains "CREATE TABLE interview_notes" with "interview_id UUID NOT NULL REFERENCES interviews(id)"
    - enums.kt contains "enum class InterviewType { PHONE, VIDEO, ONSITE, TAKE_HOME }"
    - enums.kt contains "enum class InterviewStage { SCREENING, TECHNICAL, BEHAVIORAL, CULTURE_FIT, FINAL, SYSTEM_DESIGN, HOMEWORK, OTHER }"
    - enums.kt contains "enum class InterviewOutcome { SCHEDULED, COMPLETED, CANCELLED, NO_SHOW }"
    - enums.kt contains "enum class InterviewResult { PASSED, FAILED, PENDING, MIXED }"
    - enums.kt contains "enum class InterviewNoteType { PREPARATION, QUESTION_ASKED, FEEDBACK, FOLLOW_UP, GENERAL }"
    - InterviewEntity.kt contains '@Table(name = "interviews")'
    - InterviewEntity.kt contains "val applicationId: UUID"
    - InterviewEntity.kt contains "var scheduledAt: Instant"
    - InterviewEntity.kt contains "var interviewType: InterviewType"
    - InterviewEntity.kt contains "var stage: InterviewStage"
    - InterviewEntity.kt contains "var outcome: InterviewOutcome"
    - InterviewEntity.kt contains "var result: InterviewResult"
    - InterviewNoteEntity.kt contains '@Table(name = "interview_notes")'
    - InterviewNoteEntity.kt contains "val interviewId: UUID"
    - InterviewNoteEntity.kt contains "var noteType: InterviewNoteType"
    - InterviewNoteEntity.kt does NOT contain "userId"
    - `./gradlew :backend:classes` exits 0
  </acceptance_criteria>
  <done>Both Flyway migrations exist, 5 enums added, InterviewEntity and InterviewNoteEntity compile, schema validation passes against migrations.</done>
</task>

<task type="auto">
  <name>Task 2: Repositories and DTO contracts</name>
  <files>
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/InterviewRepository.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/InterviewNoteRepository.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/InterviewDtos.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/InterviewNoteDtos.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/TimelineDtos.kt
  </files>
  <read_first>
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/ApplicationNoteRepository.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/repository/ApplicationRepository.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/ApplicationNoteDtos.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/dto/ApplicationDtos.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewEntity.kt,
    backend/src/main/kotlin/com/alex/job/hunt/jobhunt/entity/InterviewNoteEntity.kt
  </read_first>
  <action>
**1. Create InterviewRepository.kt:**
```kotlin
package com.alex.job.hunt.jobhunt.repository

import com.alex.job.hunt.jobhunt.entity.InterviewEntity
import org.springframework.data.domain.Page
import org.springframework.data.domain.Pageable
import org.springframework.data.jpa.repository.JpaRepository
import org.springframework.data.jpa.repository.Query
import java.util.UUID

interface InterviewRepository : JpaRepository<InterviewEntity, UUID> {
    fun findByIdAndUserId(id: UUID, userId: UUID): InterviewEntity?
    fun findByApplicationIdAndUserIdAndArchivedFalseOrderByScheduledAtAsc(applicationId: UUID, userId: UUID, pageable: Pageable): Page<InterviewEntity>
    fun findByApplicationIdAndArchivedFalse(applicationId: UUID): List<InterviewEntity>
    fun countByApplicationIdAndArchivedFalse(applicationId: UUID): Int

    @Query("SELECT i.id FROM InterviewEntity i WHERE i.applicationId = :applicationId AND i.archived = false")
    fun findIdsByApplicationId(applicationId: UUID): List<UUID>
}
```

**2. Create InterviewNoteRepository.kt:**
```kotlin
package com.alex.job.hunt.jobhunt.repository

import com.alex.job.hunt.jobhunt.entity.InterviewNoteEntity
import org.springframework.data.domain.Page
import org.springframework.data.domain.Pageable
import org.springframework.data.jpa.repository.JpaRepository
import java.util.UUID

interface InterviewNoteRepository : JpaRepository<InterviewNoteEntity, UUID> {
    fun findByIdAndInterviewId(id: UUID, interviewId: UUID): InterviewNoteEntity?
    fun findByInterviewIdOrderByCreatedAtDesc(interviewId: UUID, pageable: Pageable): Page<InterviewNoteEntity>
    fun findByInterviewIdOrderByCreatedAtDesc(interviewId: UUID): List<InterviewNoteEntity>
    fun findByInterviewIdIn(interviewIds: List<UUID>): List<InterviewNoteEntity>
}
```

**3. Create InterviewDtos.kt:**
```kotlin
package com.alex.job.hunt.jobhunt.dto

import com.alex.job.hunt.jobhunt.entity.InterviewType
import com.alex.job.hunt.jobhunt.entity.InterviewStage
import com.alex.job.hunt.jobhunt.entity.InterviewOutcome
import com.alex.job.hunt.jobhunt.entity.InterviewResult
import jakarta.validation.constraints.Min
import jakarta.validation.constraints.NotNull
import jakarta.validation.constraints.Size
import java.time.Instant
import java.util.UUID

data class CreateInterviewRequest(
    @field:NotNull(message = "Application ID is required")
    val applicationId: UUID,
    @field:NotNull(message = "Scheduled time is required")
    val scheduledAt: Instant,
    @field:NotNull(message = "Interview type is required")
    val interviewType: InterviewType,
    @field:NotNull(message = "Stage is required")
    val stage: InterviewStage,
    val stageLabel: String? = null,
    @field:Min(1, message = "Duration must be at least 1 minute")
    val durationMinutes: Int? = 60,
    val location: String? = null,
    @field:Size(max = 500, message = "Interviewer names must not exceed 500 characters")
    val interviewerNames: String? = null
)

data class UpdateInterviewRequest(
    val scheduledAt: Instant? = null,
    val interviewType: InterviewType? = null,
    val stage: InterviewStage? = null,
    val stageLabel: String? = null,
    val durationMinutes: Int? = null,
    val location: String? = null,
    val interviewerNames: String? = null,
    val outcome: InterviewOutcome? = null,
    val result: InterviewResult? = null,
    val candidateFeedback: String? = null,
    val companyFeedback: String? = null
)

data class InterviewResponse(
    val id: UUID,
    val applicationId: UUID,
    val roundNumber: Int,
    val scheduledAt: Instant,
    val durationMinutes: Int?,
    val interviewType: InterviewType,
    val stage: InterviewStage,
    val stageLabel: String?,
    val outcome: InterviewOutcome,
    val result: InterviewResult,
    val location: String?,
    val interviewerNames: String?,
    val candidateFeedback: String?,
    val companyFeedback: String?,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

**4. Create InterviewNoteDtos.kt:**
```kotlin
package com.alex.job.hunt.jobhunt.dto

import com.alex.job.hunt.jobhunt.entity.InterviewNoteType
import jakarta.validation.constraints.NotBlank
import jakarta.validation.constraints.Size
import java.time.Instant
import java.util.UUID

data class CreateInterviewNoteRequest(
    @field:NotBlank(message = "Content is required")
    @field:Size(max = 10000, message = "Content must not exceed 10000 characters")
    val content: String,
    val noteType: InterviewNoteType? = null
)

data class UpdateInterviewNoteRequest(
    @field:NotBlank(message = "Content is required")
    @field:Size(max = 10000, message = "Content must not exceed 10000 characters")
    val content: String
)

data class InterviewNoteResponse(
    val id: UUID,
    val interviewId: UUID,
    val content: String,
    val noteType: InterviewNoteType,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

**5. Create TimelineDtos.kt:**
```kotlin
package com.alex.job.hunt.jobhunt.dto

import java.time.Instant
import java.util.UUID

enum class TimelineEntryType { INTERVIEW, APPLICATION_NOTE, INTERVIEW_NOTE }

data class TimelineEntry(
    val id: UUID,
    val date: Instant,
    val type: TimelineEntryType,
    val summary: String,
    val details: Map<String, Any?>? = null
)
```
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt && ./gradlew :backend:classes</automated>
  </verify>
  <acceptance_criteria>
    - InterviewRepository.kt contains "interface InterviewRepository : JpaRepository<InterviewEntity, UUID>"
    - InterviewRepository.kt contains "fun findByIdAndUserId(id: UUID, userId: UUID): InterviewEntity?"
    - InterviewRepository.kt contains "fun findIdsByApplicationId(applicationId: UUID): List<UUID>"
    - InterviewRepository.kt contains "fun countByApplicationIdAndArchivedFalse(applicationId: UUID): Int"
    - InterviewNoteRepository.kt contains "interface InterviewNoteRepository : JpaRepository<InterviewNoteEntity, UUID>"
    - InterviewNoteRepository.kt contains "fun findByInterviewIdIn(interviewIds: List<UUID>): List<InterviewNoteEntity>"
    - InterviewDtos.kt contains "data class CreateInterviewRequest"
    - InterviewDtos.kt contains "data class UpdateInterviewRequest"
    - InterviewDtos.kt contains "data class InterviewResponse"
    - InterviewDtos.kt contains "val applicationId: UUID" in CreateInterviewRequest
    - InterviewDtos.kt contains "val scheduledAt: Instant" in CreateInterviewRequest
    - InterviewNoteDtos.kt contains "data class CreateInterviewNoteRequest"
    - InterviewNoteDtos.kt contains "data class InterviewNoteResponse"
    - TimelineDtos.kt contains "enum class TimelineEntryType { INTERVIEW, APPLICATION_NOTE, INTERVIEW_NOTE }"
    - TimelineDtos.kt contains "data class TimelineEntry"
    - `./gradlew :backend:classes` exits 0
  </acceptance_criteria>
  <done>All repositories and DTOs compile. InterviewRepository has query methods for CRUD, timeline ID lookup, and round counting. DTOs define complete request/response contracts.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:classes` compiles without errors
- All 10 new files exist in the expected paths
- enums.kt contains all 5 new enum classes alongside existing enums
</verification>

<success_criteria>
- Flyway V11 and V12 migrations define interviews and interview_notes tables
- InterviewEntity maps all fields from CONTEXT.md decisions (scheduledAt, durationMinutes, interviewType, stage, stageLabel, outcome, result, location, interviewerNames, candidateFeedback, companyFeedback, archived, archivedAt)
- InterviewNoteEntity follows ApplicationNoteEntity pattern with interviewId FK (no userId)
- Repositories provide all query methods needed for Plan 02 services
- DTOs establish contracts for all API endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/05-interview-management/05-01-SUMMARY.md`
</output>
