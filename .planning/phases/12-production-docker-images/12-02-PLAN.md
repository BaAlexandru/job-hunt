---
phase: 12-production-docker-images
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/Dockerfile
  - frontend/.dockerignore
  - frontend/next.config.ts
  - compose.prod.yaml
  - infra/CLAUDE.md
autonomous: true
requirements:
  - DOCK-02

must_haves:
  truths:
    - "Frontend Docker image builds successfully from frontend/ directory"
    - "Frontend Docker image is under 200MB"
    - "Frontend container starts and serves pages on port 3000"
    - "Frontend container runs as non-root user"
    - "Both backend and frontend run together via compose.prod.yaml"
  artifacts:
    - path: "frontend/Dockerfile"
      provides: "Three-stage build: deps, builder, runner with standalone output"
      contains: "node:22-alpine"
    - path: "frontend/.dockerignore"
      provides: "Build context exclusions"
      contains: ".next/"
    - path: "frontend/next.config.ts"
      provides: "Standalone output mode for Docker"
      contains: 'output: "standalone"'
    - path: "compose.prod.yaml"
      provides: "Local production testing with all services"
      contains: "backend/Dockerfile"
    - path: "infra/CLAUDE.md"
      provides: "Updated infrastructure conventions"
      contains: "backend/Dockerfile"
  key_links:
    - from: "frontend/next.config.ts"
      to: "frontend/Dockerfile"
      via: "output: standalone enables .next/standalone directory"
      pattern: 'output.*standalone'
    - from: "frontend/Dockerfile"
      to: ".next/standalone"
      via: "COPY --from=builder in runner stage"
      pattern: "COPY.*\\.next/standalone"
    - from: "compose.prod.yaml"
      to: "compose.yaml"
      via: "extends existing dev services (postgres, redis, minio)"
      pattern: "backend/Dockerfile"
---

<objective>
Create the frontend production Docker image using a three-stage build with Node.js 22 Alpine and Next.js standalone output. Also create compose.prod.yaml for local production testing of both images together, and update infra/CLAUDE.md.

Purpose: DOCK-02 -- frontend produces an optimized container image; compose.prod.yaml validates both images work end-to-end.
Output: `frontend/Dockerfile`, `frontend/.dockerignore`, updated `frontend/next.config.ts`, `compose.prod.yaml`, updated `infra/CLAUDE.md`
</objective>

<execution_context>
@C:/Users/balab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/balab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-docker-images/12-CONTEXT.md
@.planning/phases/12-production-docker-images/12-RESEARCH.md

<interfaces>
<!-- Current frontend config that must be modified -->

From frontend/next.config.ts:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["192.168.178.49"],
};

export default nextConfig;
```

From frontend/package.json (relevant fields):
```json
{
  "name": "frontend",
  "scripts": {
    "build": "next build",
    "start": "next start"
  }
}
```
Note: package.json does NOT have a "packageManager" field. corepack enable pnpm will use latest pnpm unless packageManager is set.

From compose.yaml (existing dev services to extend):
```yaml
services:
  postgres:
    image: 'postgres:17'
    environment:
      POSTGRES_DB: jobhunt
      POSTGRES_USER: jobhunt
      POSTGRES_PASSWORD: jobhunt
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
  redis:
    image: 'redis:7-alpine'
    ports:
      - '6379:6379'
  minio:
    image: 'minio/minio:latest'
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
volumes:
  pgdata:
  miniodata:
```

From backend/src/main/resources/application.yml (storage config properties for compose.prod.yaml):
```yaml
storage:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: jobhunt-documents
  region: us-east-1
```
These map to environment variables: STORAGE_ENDPOINT, STORAGE_ACCESS_KEY, STORAGE_SECRET_KEY, STORAGE_BUCKET, STORAGE_REGION
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add standalone output to next.config.ts and create frontend Dockerfile</name>
  <files>frontend/next.config.ts, frontend/Dockerfile, frontend/.dockerignore</files>
  <read_first>
    - frontend/next.config.ts
    - frontend/package.json
    - .planning/phases/12-production-docker-images/12-RESEARCH.md
  </read_first>
  <action>
**Update `frontend/next.config.ts`** to add `output: "standalone"`:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
  allowedDevOrigins: ["192.168.178.49"],
};

export default nextConfig;
```

**Create `frontend/Dockerfile`** with three stages:

**Stage 1 -- Dependencies (node:22-alpine AS deps):**
```dockerfile
FROM node:22-alpine AS deps
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN corepack enable pnpm && pnpm install --frozen-lockfile
```

**Stage 2 -- Builder (node:22-alpine AS builder):**
```dockerfile
FROM node:22-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
ENV NODE_ENV=production
ARG NEXT_PUBLIC_API_URL=http://localhost:8080/api
ENV NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
RUN corepack enable pnpm && pnpm build
```

**Stage 3 -- Runner (node:22-alpine AS runner):**
```dockerfile
FROM node:22-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"
RUN addgroup -S app && adduser -S app -G app
COPY --from=builder --chown=app:app /app/public ./public
RUN mkdir .next && chown app:app .next
COPY --from=builder --chown=app:app /app/.next/standalone ./
COPY --from=builder --chown=app:app /app/.next/static ./.next/static
USER app
EXPOSE 3000
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/ || exit 1
CMD ["node", "server.js"]
```

IMPORTANT: The standalone output traces only JS dependencies. `public/` and `.next/static/` must be explicitly copied or assets will 404.

**Create `frontend/.dockerignore`:**
```
# Build artifacts
.next/
node_modules/
out/

# IDE
.idea/
*.iml

# Git
.git/
.gitignore

# Tests
**/*.test.*
**/*.spec.*
vitest.config.*
coverage/

# Docker
Dockerfile
.dockerignore

# Misc
*.md
.env*
```
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt-phase-12 && docker build -f frontend/Dockerfile -t jobhunt-frontend frontend/ 2>&1 | tail -5 && docker image inspect jobhunt-frontend --format "Size: {{.Size}}" && docker run --rm jobhunt-frontend whoami</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/next.config.ts` contains `output: "standalone"`
    - `frontend/next.config.ts` contains `allowedDevOrigins` (preserves existing config)
    - `frontend/Dockerfile` contains `FROM node:22-alpine AS deps`
    - `frontend/Dockerfile` contains `FROM node:22-alpine AS builder`
    - `frontend/Dockerfile` contains `FROM node:22-alpine AS runner`
    - `frontend/Dockerfile` contains `corepack enable pnpm`
    - `frontend/Dockerfile` contains `pnpm install --frozen-lockfile`
    - `frontend/Dockerfile` contains `ARG NEXT_PUBLIC_API_URL=http://localhost:8080/api`
    - `frontend/Dockerfile` contains `COPY --from=builder --chown=app:app /app/.next/standalone`
    - `frontend/Dockerfile` contains `COPY --from=builder --chown=app:app /app/.next/static ./.next/static`
    - `frontend/Dockerfile` contains `COPY --from=builder --chown=app:app /app/public ./public`
    - `frontend/Dockerfile` contains `USER app`
    - `frontend/Dockerfile` contains `HEALTHCHECK`
    - `frontend/Dockerfile` contains `CMD ["node", "server.js"]`
    - `frontend/Dockerfile` contains `EXPOSE 3000`
    - `frontend/.dockerignore` contains `.next/`
    - `frontend/.dockerignore` contains `node_modules/`
    - `docker build -f frontend/Dockerfile -t jobhunt-frontend frontend/` exits 0
    - `docker image inspect jobhunt-frontend --format '{{.Size}}'` reports less than 209715200 (200MB)
    - `docker run --rm jobhunt-frontend whoami` outputs `app`
  </acceptance_criteria>
  <done>Frontend Docker image builds from frontend/ directory, is under 200MB, uses standalone output, runs as non-root user, includes HEALTHCHECK</done>
</task>

<task type="auto">
  <name>Task 2: Create compose.prod.yaml and update infra/CLAUDE.md</name>
  <files>compose.prod.yaml, infra/CLAUDE.md</files>
  <read_first>
    - compose.yaml
    - infra/CLAUDE.md
    - backend/src/main/resources/application.yml
  </read_first>
  <action>
**Create `compose.prod.yaml`** at project root. This file extends compose.yaml and adds backend + frontend services for local production testing. Run with: `docker compose -f compose.yaml -f compose.prod.yaml up --build`

```yaml
# compose.prod.yaml -- extends compose.yaml for local production testing
# Run: docker compose -f compose.yaml -f compose.prod.yaml up --build
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/jobhunt
      SPRING_DATASOURCE_USERNAME: jobhunt
      SPRING_DATASOURCE_PASSWORD: jobhunt
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: "6379"
      STORAGE_ENDPOINT: http://minio:9000
      STORAGE_ACCESS_KEY: minioadmin
      STORAGE_SECRET_KEY: minioadmin
      STORAGE_BUCKET: jobhunt-documents
      STORAGE_REGION: us-east-1
      JWT_SECRET: dGhpcy1pcy1hLWxvY2FsLWRldi1zZWNyZXQta2V5LXRoYXQtaXMtbG9uZy1lbm91Z2gtZm9yLUhTMjU2
    depends_on:
      postgres:
        condition: service_started
      redis:
        condition: service_started
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost:8080/api
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
```

Key details:
- `STORAGE_ENDPOINT` points to `http://minio:9000` (Docker network hostname, not localhost)
- `SPRING_DATASOURCE_URL` uses `postgres` hostname (Docker network)
- `SPRING_DATA_REDIS_HOST` uses `redis` hostname (Docker network)
- Backend depends on postgres, redis, minio (from compose.yaml) with appropriate conditions
- Frontend depends on backend being healthy
- `NEXT_PUBLIC_API_URL` is set to `http://localhost:8080/api` as a build arg (browser-side URL, not Docker-internal)
- JWT_SECRET uses the same dev secret from application.yml default

**Update `infra/CLAUDE.md`** -- the file has already been partially updated. Read the current contents and ensure it reflects the final state. The file should already contain the correct Dockerfile placement info from the context session. No changes needed if it already matches the current state shown in read_first.
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt-phase-12 && grep -c "backend/Dockerfile" compose.prod.yaml && grep "STORAGE_ENDPOINT" compose.prod.yaml && grep "minio:9000" compose.prod.yaml && grep "SPRING_DATASOURCE_URL" compose.prod.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `compose.prod.yaml` exists at project root
    - `compose.prod.yaml` contains `dockerfile: backend/Dockerfile`
    - `compose.prod.yaml` contains `context: .` (for backend service)
    - `compose.prod.yaml` contains `context: frontend` (for frontend service)
    - `compose.prod.yaml` contains `SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/jobhunt`
    - `compose.prod.yaml` contains `SPRING_DATASOURCE_USERNAME: jobhunt`
    - `compose.prod.yaml` contains `SPRING_DATA_REDIS_HOST: redis`
    - `compose.prod.yaml` contains `STORAGE_ENDPOINT: http://minio:9000`
    - `compose.prod.yaml` contains `STORAGE_ACCESS_KEY: minioadmin`
    - `compose.prod.yaml` contains `STORAGE_BUCKET: jobhunt-documents`
    - `compose.prod.yaml` contains `JWT_SECRET:`
    - `compose.prod.yaml` contains `NEXT_PUBLIC_API_URL: http://localhost:8080/api`
    - `compose.prod.yaml` contains `condition: service_healthy` (for backend dependency on frontend)
    - `compose.prod.yaml` contains `start_period: 60s` (backend healthcheck)
    - `infra/CLAUDE.md` contains `backend/Dockerfile`
  </acceptance_criteria>
  <done>compose.prod.yaml extends compose.yaml with backend and frontend services using correct Docker network hostnames for all service connections. infra/CLAUDE.md reflects module-root Dockerfile placement.</done>
</task>

</tasks>

<verification>
- `docker build -f frontend/Dockerfile -t jobhunt-frontend frontend/` completes without errors
- `docker image inspect jobhunt-frontend --format '{{.Size}}'` shows value under 209715200 (200MB)
- `docker run --rm jobhunt-frontend whoami` outputs `app`
- `grep -c 'FROM' frontend/Dockerfile` returns 3 (three stages)
- `grep 'output.*standalone' frontend/next.config.ts` matches
- `compose.prod.yaml` contains both backend and frontend services with correct environment variables
- Full integration: `docker compose -f compose.yaml -f compose.prod.yaml up --build -d` starts all services (manual verification)
</verification>

<success_criteria>
- Frontend Docker image builds from frontend/ directory with `docker build -f frontend/Dockerfile frontend/`
- Image size is under 200MB
- Container runs as non-root `app` user
- HEALTHCHECK instruction is present
- next.config.ts has `output: "standalone"` added
- .dockerignore excludes .next/, node_modules/, test files
- compose.prod.yaml extends compose.yaml with both app services
- compose.prod.yaml uses Docker network hostnames (postgres, redis, minio) not localhost
- infra/CLAUDE.md reflects current Dockerfile placement conventions
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-docker-images/12-02-SUMMARY.md`
</output>
