---
phase: 17-app-deployment-argocd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/argocd/values.yaml
  - infra/argocd/root-app.yaml
  - infra/argocd/apps/staging.yaml
  - infra/argocd/apps/prod.yaml
  - infra/scripts/seal-secrets.sh
  - infra/scripts/backup-sealed-key.sh
  - infra/k8s/overlays/prod/kustomization.yaml
  - infra/k8s/overlays/staging/kustomization.yaml
autonomous: true
requirements: [ARGO-01, ARGO-02, ARGO-03]

must_haves:
  truths:
    - "ArgoCD Helm values file exists with resource limits targeting ~400MB total and unused components disabled"
    - "App-of-apps pattern defined: root Application + staging child + prod child"
    - "Staging Application has automated syncPolicy with selfHeal; prod Application has no automated sync"
    - "seal-secrets.sh generates random passwords and pipes through kubeseal without writing intermediate plaintext"
    - "Kustomize overlays reference sealed-secret YAML files instead of plaintext secrets.yaml"
  artifacts:
    - path: "infra/argocd/values.yaml"
      provides: "ArgoCD Helm chart overrides with resource limits"
      contains: "notifications"
    - path: "infra/argocd/root-app.yaml"
      provides: "Root Application pointing to infra/argocd/apps"
      contains: "kind: Application"
    - path: "infra/argocd/apps/staging.yaml"
      provides: "Staging child Application with auto-sync"
      contains: "selfHeal: true"
    - path: "infra/argocd/apps/prod.yaml"
      provides: "Prod child Application without auto-sync"
      contains: "kind: Application"
    - path: "infra/scripts/seal-secrets.sh"
      provides: "Secret sealing helper"
      contains: "kubeseal"
    - path: "infra/scripts/backup-sealed-key.sh"
      provides: "Controller key backup script"
      contains: "sealedsecrets.bitnami.com"
    - path: "infra/k8s/overlays/prod/kustomization.yaml"
      provides: "Prod overlay without plaintext secrets"
      contains: "sealed-secret"
    - path: "infra/k8s/overlays/staging/kustomization.yaml"
      provides: "Staging overlay without plaintext secrets"
      contains: "sealed-secret"
  key_links:
    - from: "infra/argocd/root-app.yaml"
      to: "infra/argocd/apps/"
      via: "spec.source.path"
      pattern: "path: infra/argocd/apps"
    - from: "infra/argocd/apps/staging.yaml"
      to: "infra/k8s/overlays/staging"
      via: "spec.source.path"
      pattern: "path: infra/k8s/overlays/staging"
    - from: "infra/argocd/apps/prod.yaml"
      to: "infra/k8s/overlays/prod"
      via: "spec.source.path"
      pattern: "path: infra/k8s/overlays/prod"
    - from: "infra/scripts/seal-secrets.sh"
      to: "infra/k8s/overlays/{env}/"
      via: "kubeseal output path"
      pattern: "sealed-secret.yaml"
---

<objective>
Create ArgoCD Helm values, app-of-apps Application manifests, Sealed Secrets helper scripts, and update Kustomize overlays to reference sealed secrets instead of plaintext.

Purpose: Establishes the GitOps file structure that ArgoCD will manage. After this plan, all manifests exist in Git ready for cluster-side installation. Sealed Secrets scripts enable secure credential management without plaintext in the repository.

Output: ArgoCD values.yaml, 3 Application CRDs (root + 2 children), seal-secrets.sh, backup-sealed-key.sh, updated overlay kustomization.yaml files (secrets.yaml replaced with sealed-secret placeholder references).
</objective>

<execution_context>
@C:/Users/balab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/balab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-app-deployment-argocd/17-CONTEXT.md
@.planning/phases/17-app-deployment-argocd/17-RESEARCH.md

<interfaces>
<!-- Existing overlay structure that will be modified -->

From infra/k8s/overlays/prod/kustomization.yaml:
```yaml
resources:
  - ../../base
  - configmap.yaml
  - secrets.yaml       # TO BE REPLACED with sealed-secret references
  - ingress.yaml
images:
  - name: jobhunt-backend
    newName: ghcr.io/baalexandru/jobhunt-backend
    newTag: latest
  - name: jobhunt-frontend
    newName: ghcr.io/baalexandru/jobhunt-frontend
    newTag: latest
```

From infra/k8s/overlays/prod/secrets.yaml (3 secrets to seal):
- backend-secrets: DB_USERNAME, DB_PASSWORD, JWT_SECRET, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, SMTP_USERNAME, SMTP_PASSWORD, INTERNAL_API_SECRET
- postgres-secrets: POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD
- minio-secrets: MINIO_ROOT_USER, MINIO_ROOT_PASSWORD

Secret names that envFrom references in base deployments:
- backend-secrets (backend deployment)
- postgres-secrets (postgres statefulset)
- minio-secrets (minio statefulset)
</interfaces>
</context>

<prerequisites>
## Manual Cluster-Side Prerequisites (before ArgoCD can sync)

These steps happen on the live cluster during execution, not as file creation:

1. **SSH deploy key for ArgoCD Git access:**
   - Generate an Ed25519 key pair: `ssh-keygen -t ed25519 -C "argocd-deploy-key" -f argocd-deploy-key -N ""`
   - Add the **public** key (`argocd-deploy-key.pub`) to GitHub repo → Settings → Deploy keys (read-only)
   - Create a K8s Secret in the `argocd` namespace with the **private** key and the required label:
     ```yaml
     apiVersion: v1
     kind: Secret
     metadata:
       name: repo-job-hunt
       namespace: argocd
       labels:
         argocd.argoproj.io/secret-type: repository
     type: Opaque
     stringData:
       type: git
       url: git@github.com:baalexandru/job-hunt.git
       sshPrivateKey: |
         <contents of argocd-deploy-key>
     ```
   - Apply via: `kubectl apply -f <secret-file>` (do NOT commit the private key to Git)
   - Verify: `argocd repo list` (or check ArgoCD UI → Settings → Repositories)

2. **Sealed Secrets controller must be running** before `seal-secrets.sh` can execute (Helm install happens during execution)

3. **ArgoCD must be installed** before applying the root Application (Helm install happens during execution)
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Create ArgoCD Helm values and app-of-apps Application manifests</name>
  <files>infra/argocd/values.yaml, infra/argocd/root-app.yaml, infra/argocd/apps/staging.yaml, infra/argocd/apps/prod.yaml</files>
  <read_first>
    - infra/k8s/overlays/prod/kustomization.yaml
    - infra/k8s/overlays/staging/kustomization.yaml
    - .planning/phases/17-app-deployment-argocd/17-RESEARCH.md
  </read_first>
  <action>
Create 4 files in the `infra/argocd/` directory structure:

**1. `infra/argocd/values.yaml`** -- ArgoCD Helm chart overrides for constrained 2GB cluster:
```yaml
global:
  logging:
    level: warn

notifications:
  enabled: false

applicationSet:
  enabled: false

dex:
  enabled: false

server:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 500m
      memory: 128Mi

controller:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi

repoServer:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 500m
      memory: 192Mi

redis:
  resources:
    requests:
      cpu: 50m
      memory: 32Mi
    limits:
      cpu: 250m
      memory: 64Mi
```

**2. `infra/argocd/root-app.yaml`** -- Root Application (app-of-apps):
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobhunt
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:baalexandru/job-hunt.git
    targetRevision: master
    path: infra/argocd/apps
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
```

**3. `infra/argocd/apps/staging.yaml`** -- Staging child Application with auto-sync + selfHeal:
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobhunt-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:baalexandru/job-hunt.git
    targetRevision: master
    path: infra/k8s/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: jobhunt-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
```

**4. `infra/argocd/apps/prod.yaml`** -- Production child Application with NO auto-sync (manual promotion only):
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobhunt-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:baalexandru/job-hunt.git
    targetRevision: master
    path: infra/k8s/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: jobhunt-prod
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
```

Note: prod.yaml intentionally has NO `syncPolicy.automated` block -- production requires manual sync in ArgoCD UI or CLI.
  </action>
  <verify>
    <automated>test -f infra/argocd/values.yaml && test -f infra/argocd/root-app.yaml && test -f infra/argocd/apps/staging.yaml && test -f infra/argocd/apps/prod.yaml && echo "All 4 ArgoCD files exist"</automated>
  </verify>
  <acceptance_criteria>
    - infra/argocd/values.yaml contains `notifications:` with `enabled: false`
    - infra/argocd/values.yaml contains `applicationSet:` with `enabled: false`
    - infra/argocd/values.yaml contains `dex:` with `enabled: false`
    - infra/argocd/values.yaml contains `memory: 256Mi` (controller limit)
    - infra/argocd/root-app.yaml contains `name: jobhunt` and `path: infra/argocd/apps`
    - infra/argocd/root-app.yaml contains `selfHeal: true`
    - infra/argocd/apps/staging.yaml contains `name: jobhunt-staging` and `path: infra/k8s/overlays/staging`
    - infra/argocd/apps/staging.yaml contains `selfHeal: true` under `automated:`
    - infra/argocd/apps/prod.yaml contains `name: jobhunt-prod` and `path: infra/k8s/overlays/prod`
    - infra/argocd/apps/prod.yaml does NOT contain `automated:` (manual sync only)
    - All Application manifests use `repoURL: git@github.com:baalexandru/job-hunt.git`
    - All Application manifests use `targetRevision: master`
    - prod.yaml and staging.yaml both have `CreateNamespace=false` in syncOptions
    - prod.yaml and staging.yaml both have `resources-finalizer.argocd.argoproj.io` in finalizers
  </acceptance_criteria>
  <done>ArgoCD Helm values exist with resource limits and disabled components. Three Application CRDs define the app-of-apps pattern: root -> staging (auto-sync) + prod (manual sync).</done>
</task>

<task type="auto">
  <name>Task 2: Create Sealed Secrets scripts and update overlay kustomizations</name>
  <files>infra/scripts/seal-secrets.sh, infra/scripts/backup-sealed-key.sh, infra/k8s/overlays/prod/kustomization.yaml, infra/k8s/overlays/staging/kustomization.yaml</files>
  <read_first>
    - infra/k8s/overlays/prod/kustomization.yaml
    - infra/k8s/overlays/staging/kustomization.yaml
    - infra/k8s/overlays/prod/secrets.yaml
    - infra/k8s/overlays/staging/secrets.yaml
    - .planning/phases/17-app-deployment-argocd/17-RESEARCH.md
  </read_first>
  <action>
Create 2 scripts and update 2 kustomization files:

**1. `infra/scripts/seal-secrets.sh`** -- generates random passwords, creates Secret YAML via `kubectl create secret --dry-run=client`, pipes through `kubeseal`, writes SealedSecret files. Must NOT write intermediate plaintext files to disk.

Script structure:
```bash
#!/usr/bin/env bash
# Usage: ./seal-secrets.sh <staging|prod>
# Generates random passwords, creates Secret YAML, pipes through kubeseal
# NEVER writes plaintext secrets to disk

set -euo pipefail

ENV="${1:?Usage: seal-secrets.sh <staging|prod>}"
NAMESPACE="jobhunt-${ENV}"
CONTROLLER_NAME="sealed-secrets"
CONTROLLER_NAMESPACE="kube-system"

# Generate random values
DB_PASSWORD=$(openssl rand -base64 24)
JWT_SECRET=$(openssl rand -base64 32)
MINIO_ACCESS_KEY=$(openssl rand -base64 16)
MINIO_SECRET_KEY=$(openssl rand -base64 32)
INTERNAL_API_SECRET=$(openssl rand -base64 32)
POSTGRES_PASSWORD="$DB_PASSWORD"
MINIO_ROOT_USER="$MINIO_ACCESS_KEY"
MINIO_ROOT_PASSWORD="$MINIO_SECRET_KEY"

OUTPUT_DIR="infra/k8s/overlays/${ENV}"

# Backend secrets (8 keys)
kubectl create secret generic backend-secrets \
  --namespace="$NAMESPACE" \
  --from-literal=DB_USERNAME=jobhunt \
  --from-literal=DB_PASSWORD="$DB_PASSWORD" \
  --from-literal=JWT_SECRET="$JWT_SECRET" \
  --from-literal=MINIO_ACCESS_KEY="$MINIO_ACCESS_KEY" \
  --from-literal=MINIO_SECRET_KEY="$MINIO_SECRET_KEY" \
  --from-literal=SMTP_USERNAME=changeme \
  --from-literal=SMTP_PASSWORD=changeme \
  --from-literal=INTERNAL_API_SECRET="$INTERNAL_API_SECRET" \
  --dry-run=client -o yaml | \
kubeseal --controller-name="$CONTROLLER_NAME" \
  --controller-namespace="$CONTROLLER_NAMESPACE" \
  --namespace="$NAMESPACE" \
  --format=yaml > "${OUTPUT_DIR}/backend-sealed-secret.yaml"

# Postgres secrets (3 keys)
kubectl create secret generic postgres-secrets \
  --namespace="$NAMESPACE" \
  --from-literal=POSTGRES_DB=jobhunt \
  --from-literal=POSTGRES_USER=jobhunt \
  --from-literal=POSTGRES_PASSWORD="$POSTGRES_PASSWORD" \
  --dry-run=client -o yaml | \
kubeseal --controller-name="$CONTROLLER_NAME" \
  --controller-namespace="$CONTROLLER_NAMESPACE" \
  --namespace="$NAMESPACE" \
  --format=yaml > "${OUTPUT_DIR}/postgres-sealed-secret.yaml"

# MinIO secrets (2 keys)
kubectl create secret generic minio-secrets \
  --namespace="$NAMESPACE" \
  --from-literal=MINIO_ROOT_USER="$MINIO_ROOT_USER" \
  --from-literal=MINIO_ROOT_PASSWORD="$MINIO_ROOT_PASSWORD" \
  --dry-run=client -o yaml | \
kubeseal --controller-name="$CONTROLLER_NAME" \
  --controller-namespace="$CONTROLLER_NAMESPACE" \
  --namespace="$NAMESPACE" \
  --format=yaml > "${OUTPUT_DIR}/minio-sealed-secret.yaml"

echo "Sealed secrets written to ${OUTPUT_DIR}/"
echo "IMPORTANT: DB_PASSWORD and POSTGRES_PASSWORD match: $DB_PASSWORD"
echo "IMPORTANT: MINIO_ACCESS_KEY and MINIO_ROOT_USER match: $MINIO_ACCESS_KEY"
```

Make executable: `chmod +x infra/scripts/seal-secrets.sh`

**2. `infra/scripts/backup-sealed-key.sh`** -- exports the Sealed Secrets controller signing key pair for disaster recovery:
```bash
#!/usr/bin/env bash
# backup-sealed-key.sh -- Export the controller's signing key pair
# Store the output file securely OUTSIDE of Git
set -euo pipefail
BACKUP_DIR="${1:-$HOME/.sealed-secrets-backup}"
mkdir -p "$BACKUP_DIR"
kubectl get secret -n kube-system -l sealedsecrets.bitnami.com/sealed-secrets-key \
  -o yaml > "${BACKUP_DIR}/sealed-secrets-key-$(date +%Y%m%d).yaml"
echo "Key backed up to ${BACKUP_DIR}/"
echo "IMPORTANT: Store this file securely outside Git."
```

Make executable: `chmod +x infra/scripts/backup-sealed-key.sh`

**3. Update `infra/k8s/overlays/prod/kustomization.yaml`** -- replace `secrets.yaml` with three sealed-secret YAML file references. Keep everything else (namespace, images, replicas) unchanged:
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jobhunt-prod

resources:
  - ../../base
  - configmap.yaml
  - backend-sealed-secret.yaml
  - postgres-sealed-secret.yaml
  - minio-sealed-secret.yaml
  - ingress.yaml

images:
  - name: jobhunt-backend
    newName: ghcr.io/baalexandru/jobhunt-backend
    newTag: latest
  - name: jobhunt-frontend
    newName: ghcr.io/baalexandru/jobhunt-frontend
    newTag: latest

replicas:
  - name: backend
    count: 1
  - name: frontend
    count: 1
  - name: redis
    count: 1
  - name: postgres
    count: 1
  - name: minio
    count: 1
```

**4. Update `infra/k8s/overlays/staging/kustomization.yaml`** -- same substitution of secrets.yaml with sealed-secret references. Keep everything else (namespace, images, patches, replicas) unchanged:
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jobhunt-staging

resources:
  - ../../base
  - configmap.yaml
  - backend-sealed-secret.yaml
  - postgres-sealed-secret.yaml
  - minio-sealed-secret.yaml
  - ingress.yaml

images:
  - name: jobhunt-backend
    newName: ghcr.io/baalexandru/jobhunt-backend
    newTag: latest
  - name: jobhunt-frontend
    newName: ghcr.io/baalexandru/jobhunt-frontend
    newTag: latest

patches:
  - path: suspend-jobs.yaml

replicas:
  - name: backend
    count: 0
  - name: frontend
    count: 0
  - name: redis
    count: 0
  - name: postgres
    count: 0
  - name: minio
    count: 0
```

Do NOT delete the old `secrets.yaml` files yet -- they serve as reference for what keys each secret needs. The executor should rename them to `secrets.yaml.bak` or add a comment noting they are superseded by sealed secrets. The sealed-secret YAML files will be generated on the live cluster by running `seal-secrets.sh` during execution.

Create placeholder sealed-secret YAML files so `kustomize build` does not fail before the script is run on the cluster. Each placeholder should be a valid SealedSecret CRD with a comment indicating it needs to be regenerated:

**5. Create placeholder files** in both prod and staging overlays:
- `infra/k8s/overlays/prod/backend-sealed-secret.yaml`
- `infra/k8s/overlays/prod/postgres-sealed-secret.yaml`
- `infra/k8s/overlays/prod/minio-sealed-secret.yaml`
- `infra/k8s/overlays/staging/backend-sealed-secret.yaml`
- `infra/k8s/overlays/staging/postgres-sealed-secret.yaml`
- `infra/k8s/overlays/staging/minio-sealed-secret.yaml`

Each placeholder follows this pattern (example for backend-sealed-secret.yaml in prod):
```yaml
# PLACEHOLDER -- regenerate with: ./infra/scripts/seal-secrets.sh prod
# This file will be overwritten by kubeseal output
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: backend-secrets
  namespace: jobhunt-prod
spec:
  encryptedData: {}
  template:
    metadata:
      name: backend-secrets
    type: Opaque
```

Adjust `metadata.name`, `template.metadata.name`, `metadata.namespace` per secret and environment:
- backend-secrets / postgres-secrets / minio-secrets
- jobhunt-prod / jobhunt-staging
  </action>
  <verify>
    <automated>test -x infra/scripts/seal-secrets.sh && test -x infra/scripts/backup-sealed-key.sh && grep -q "sealed-secret" infra/k8s/overlays/prod/kustomization.yaml && grep -q "sealed-secret" infra/k8s/overlays/staging/kustomization.yaml && echo "Scripts executable, overlays updated"</automated>
  </verify>
  <acceptance_criteria>
    - infra/scripts/seal-secrets.sh is executable and contains `kubeseal --controller-name="sealed-secrets"`
    - infra/scripts/seal-secrets.sh contains `--dry-run=client -o yaml` (no plaintext written to disk)
    - infra/scripts/seal-secrets.sh generates all 3 secrets: backend-secrets, postgres-secrets, minio-secrets
    - infra/scripts/seal-secrets.sh uses `openssl rand -base64` for password generation
    - infra/scripts/seal-secrets.sh writes to `${OUTPUT_DIR}/backend-sealed-secret.yaml`, `postgres-sealed-secret.yaml`, `minio-sealed-secret.yaml`
    - infra/scripts/backup-sealed-key.sh is executable and contains `sealedsecrets.bitnami.com/sealed-secrets-key`
    - infra/k8s/overlays/prod/kustomization.yaml contains `backend-sealed-secret.yaml` and NOT `secrets.yaml` in resources
    - infra/k8s/overlays/staging/kustomization.yaml contains `backend-sealed-secret.yaml` and NOT `secrets.yaml` in resources
    - infra/k8s/overlays/prod/kustomization.yaml still has `namespace: jobhunt-prod` and all 5 replicas entries
    - infra/k8s/overlays/staging/kustomization.yaml still has `namespace: jobhunt-staging`, `suspend-jobs.yaml` patch, and all 5 replicas at count 0
    - Placeholder sealed-secret YAML files exist in both prod and staging overlays (6 files total)
    - Each placeholder contains `kind: SealedSecret` and correct namespace/name
  </acceptance_criteria>
  <done>Sealed Secrets scripts are ready for cluster-side execution. Kustomize overlays reference sealed-secret files instead of plaintext secrets. Placeholder SealedSecret files allow kustomize build to succeed before real sealing occurs.</done>
</task>

</tasks>

<verification>
- All 4 ArgoCD manifest files exist with correct Application CRD structure
- Root app points to `infra/argocd/apps`, staging app points to `infra/k8s/overlays/staging`, prod app points to `infra/k8s/overlays/prod`
- Staging has `syncPolicy.automated.selfHeal: true`, prod has NO `automated` block
- seal-secrets.sh script is executable and handles both staging and prod environments
- backup-sealed-key.sh script is executable
- Both overlay kustomization.yaml files reference sealed-secret files, not secrets.yaml
- Placeholder SealedSecret files exist so kustomize build does not break
</verification>

<success_criteria>
ArgoCD Helm values, app-of-apps Application manifests, and Sealed Secrets infrastructure exist in Git. Kustomize overlays are updated to reference encrypted secrets. All files are ready for cluster-side installation during phase execution.
</success_criteria>

<output>
After completion, create `.planning/phases/17-app-deployment-argocd/17-01-SUMMARY.md`
</output>
