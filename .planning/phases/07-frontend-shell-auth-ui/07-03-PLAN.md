---
phase: 07-frontend-shell-auth-ui
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - frontend/app/(dashboard)/layout.tsx
  - frontend/app/(dashboard)/dashboard/page.tsx
  - frontend/app/(dashboard)/applications/page.tsx
  - frontend/app/(dashboard)/companies/page.tsx
  - frontend/app/(dashboard)/jobs/page.tsx
  - frontend/app/(dashboard)/documents/page.tsx
  - frontend/components/layout/sidebar.tsx
  - frontend/components/layout/topbar.tsx
  - frontend/components/layout/mobile-nav.tsx
autonomous: false

requirements: [INFR-05]

must_haves:
  truths:
    - "Authenticated user sees a sidebar with Dashboard, Applications, Companies, Jobs, Documents links"
    - "Active nav item is visually highlighted based on current path"
    - "Top bar shows user info via UserButton with sign-out action"
    - "Sidebar is hidden below 768px and a hamburger menu appears in the top bar"
    - "Hamburger menu opens a slide-over Sheet with the same navigation links"
    - "All placeholder pages show appropriate empty state messaging"
    - "Dashboard page shows welcome message"
  artifacts:
    - path: "frontend/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with sidebar + topbar"
      contains: "Sidebar"
    - path: "frontend/components/layout/sidebar.tsx"
      provides: "Desktop navigation sidebar"
      contains: "navItems"
    - path: "frontend/components/layout/topbar.tsx"
      provides: "Top bar with UserButton and mobile hamburger"
      contains: "UserButton"
    - path: "frontend/components/layout/mobile-nav.tsx"
      provides: "Mobile navigation in Sheet component"
      contains: "Sheet"
  key_links:
    - from: "frontend/app/(dashboard)/layout.tsx"
      to: "frontend/components/layout/sidebar.tsx"
      via: "import { Sidebar }"
      pattern: "Sidebar"
    - from: "frontend/app/(dashboard)/layout.tsx"
      to: "frontend/components/layout/topbar.tsx"
      via: "import { Topbar }"
      pattern: "Topbar"
    - from: "frontend/components/layout/topbar.tsx"
      to: "@daveyplate/better-auth-ui"
      via: "UserButton component"
      pattern: "UserButton"
    - from: "frontend/components/layout/topbar.tsx"
      to: "frontend/components/layout/mobile-nav.tsx"
      via: "Sheet with MobileNav"
      pattern: "MobileNav"
---

<objective>
Build the authenticated dashboard layout with responsive sidebar navigation, top bar with UserButton, mobile hamburger menu, and all placeholder pages with empty state content.

Purpose: Complete the frontend shell so authenticated users have a fully navigable layout with all sections scaffolded, ready for Phase 8 to fill in real feature pages.
Output: Working responsive layout with sidebar (desktop) / slide-over menu (mobile), UserButton for account actions, and 5 placeholder pages.
</objective>

<execution_context>
@D:/dev/projects/job-hunt/.claude/get-shit-done/workflows/execute-plan.md
@D:/dev/projects/job-hunt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-shell-auth-ui/07-CONTEXT.md
@.planning/phases/07-frontend-shell-auth-ui/07-RESEARCH.md
@.planning/phases/07-frontend-shell-auth-ui/07-UI-SPEC.md
@.planning/phases/07-frontend-shell-auth-ui/07-01-SUMMARY.md
@.planning/phases/07-frontend-shell-auth-ui/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From frontend/lib/utils.ts:
- export function cn(...inputs: ClassValue[]): string

From frontend/components/ui/button.tsx:
- export { Button, buttonVariants }

From frontend/components/ui/sheet.tsx:
- export { Sheet, SheetContent, SheetTrigger, SheetHeader, SheetTitle }

<!-- From Plan 02 outputs -->
From frontend/components/shared/empty-state.tsx:
- export function EmptyState({ heading, body }: { heading: string; body: string })

From @daveyplate/better-auth-ui:
- UserButton: avatar dropdown with sign-out action, renders user email/name
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout, sidebar, topbar, mobile nav, and all placeholder pages</name>
  <files>
    frontend/app/(dashboard)/layout.tsx,
    frontend/app/(dashboard)/dashboard/page.tsx,
    frontend/app/(dashboard)/applications/page.tsx,
    frontend/app/(dashboard)/companies/page.tsx,
    frontend/app/(dashboard)/jobs/page.tsx,
    frontend/app/(dashboard)/documents/page.tsx,
    frontend/components/layout/sidebar.tsx,
    frontend/components/layout/topbar.tsx,
    frontend/components/layout/mobile-nav.tsx
  </files>
  <read_first>
    frontend/app/layout.tsx (confirm root layout structure),
    frontend/components/providers.tsx (confirm provider wrapping),
    frontend/components/shared/empty-state.tsx (confirm EmptyState component interface),
    frontend/lib/utils.ts (confirm cn utility),
    .planning/phases/07-frontend-shell-auth-ui/07-RESEARCH.md (Pattern 7: dashboard layout, sidebar, topbar code examples),
    .planning/phases/07-frontend-shell-auth-ui/07-UI-SPEC.md (layout contract, spacing, copywriting for nav and empty states, responsive breakpoints, interaction states)
  </read_first>
  <action>
**1. Create `frontend/components/layout/sidebar.tsx`:**
```typescript
"use client"

import { usePathname } from "next/navigation"
import Link from "next/link"
import { cn } from "@/lib/utils"
import {
  LayoutDashboard,
  Briefcase,
  Building2,
  FileText,
  FolderOpen,
} from "lucide-react"

export const navItems = [
  { href: "/dashboard", label: "Dashboard", icon: LayoutDashboard },
  { href: "/applications", label: "Applications", icon: Briefcase },
  { href: "/companies", label: "Companies", icon: Building2 },
  { href: "/jobs", label: "Jobs", icon: FileText },
  { href: "/documents", label: "Documents", icon: FolderOpen },
]

export function Sidebar() {
  const pathname = usePathname()

  return (
    <aside className="hidden w-64 flex-col border-r md:flex">
      <div className="p-6">
        <h1 className="text-xl font-semibold">JobHunt</h1>
      </div>
      <nav className="flex-1 space-y-1 px-3">
        {navItems.map((item) => (
          <Link
            key={item.href}
            href={item.href}
            className={cn(
              "flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium transition-colors",
              pathname === item.href
                ? "bg-accent text-accent-foreground"
                : "text-muted-foreground hover:bg-accent hover:text-accent-foreground"
            )}
          >
            <item.icon className="h-4 w-4" />
            {item.label}
          </Link>
        ))}
      </nav>
    </aside>
  )
}
```
- `hidden md:flex` hides sidebar below 768px (INFR-05 responsive requirement)
- Sidebar width: w-64 (256px) per UI-SPEC layout contract
- App name "JobHunt" in sidebar header per UI-SPEC copywriting
- Nav items: Dashboard, Applications, Companies, Jobs, Documents per CONTEXT.md locked decision
- Active item: `bg-accent text-accent-foreground`, inactive: `text-muted-foreground` per UI-SPEC interaction states
- Export `navItems` array so MobileNav can reuse it
- Icons: LayoutDashboard, Briefcase, Building2, FileText, FolderOpen from lucide-react

**2. Create `frontend/components/layout/mobile-nav.tsx`:**
```typescript
"use client"

import { usePathname } from "next/navigation"
import Link from "next/link"
import { cn } from "@/lib/utils"
import { navItems } from "./sidebar"

export function MobileNav({ onNavigate }: { onNavigate?: () => void }) {
  const pathname = usePathname()

  return (
    <div className="flex flex-col">
      <div className="p-6">
        <h2 className="text-xl font-semibold">JobHunt</h2>
      </div>
      <nav className="flex-1 space-y-1 px-3">
        {navItems.map((item) => (
          <Link
            key={item.href}
            href={item.href}
            onClick={onNavigate}
            className={cn(
              "flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium transition-colors",
              pathname === item.href
                ? "bg-accent text-accent-foreground"
                : "text-muted-foreground hover:bg-accent hover:text-accent-foreground"
            )}
          >
            <item.icon className="h-4 w-4" />
            {item.label}
          </Link>
        ))}
      </nav>
    </div>
  )
}
```
- Reuses `navItems` from sidebar (single source of truth)
- `onNavigate` callback closes the Sheet when a link is clicked
- Same visual styling as desktop sidebar for consistency

**3. Create `frontend/components/layout/topbar.tsx`:**
```typescript
"use client"

import { useState } from "react"
import { UserButton } from "@daveyplate/better-auth-ui"
import { Menu } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet"
import { MobileNav } from "./mobile-nav"

export function Topbar() {
  const [open, setOpen] = useState(false)

  return (
    <header className="flex h-14 items-center justify-between border-b px-6">
      <div className="md:hidden">
        <Sheet open={open} onOpenChange={setOpen}>
          <SheetTrigger asChild>
            <Button variant="ghost" size="icon">
              <Menu className="h-5 w-5" />
            </Button>
          </SheetTrigger>
          <SheetContent side="left" className="w-64 p-0">
            <MobileNav onNavigate={() => setOpen(false)} />
          </SheetContent>
        </Sheet>
      </div>
      <div className="flex-1" />
      <UserButton />
    </header>
  )
}
```
- Top bar height: h-14 (56px) per UI-SPEC layout contract
- Hamburger icon (`Menu` from lucide-react) visible only below md (768px) via `md:hidden`
- Sheet slides from left, width w-64 (256px) matching sidebar
- `onNavigate` callback closes Sheet on link click
- `UserButton` from `@daveyplate/better-auth-ui` renders avatar dropdown with sign-out action
- Controlled Sheet state so it closes when user navigates

**4. Create `frontend/app/(dashboard)/layout.tsx`:**
```typescript
import { Sidebar } from "@/components/layout/sidebar"
import { Topbar } from "@/components/layout/topbar"

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="flex h-screen">
      <Sidebar />
      <div className="flex flex-1 flex-col">
        <Topbar />
        <main className="flex-1 overflow-auto p-6">{children}</main>
      </div>
    </div>
  )
}
```
- `h-screen` for full viewport height per UI-SPEC
- Sidebar left, content right with `flex`
- Main content: `flex-1 overflow-auto p-6` (24px padding per UI-SPEC spacing)
- Route group `(dashboard)` does not affect URL paths

**5. Create placeholder pages with exact UI-SPEC copywriting:**

`frontend/app/(dashboard)/dashboard/page.tsx`:
```typescript
import { EmptyState } from "@/components/shared/empty-state"

export default function DashboardPage() {
  return (
    <EmptyState
      heading="Welcome to JobHunt"
      body="Start by adding companies and jobs you're interested in."
    />
  )
}
```

`frontend/app/(dashboard)/applications/page.tsx`:
```typescript
import { EmptyState } from "@/components/shared/empty-state"

export default function ApplicationsPage() {
  return (
    <EmptyState
      heading="No applications yet"
      body="Create a job posting first, then track your application."
    />
  )
}
```

`frontend/app/(dashboard)/companies/page.tsx`:
```typescript
import { EmptyState } from "@/components/shared/empty-state"

export default function CompaniesPage() {
  return (
    <EmptyState
      heading="No companies yet"
      body="Add companies you're applying to."
    />
  )
}
```

`frontend/app/(dashboard)/jobs/page.tsx`:
```typescript
import { EmptyState } from "@/components/shared/empty-state"

export default function JobsPage() {
  return (
    <EmptyState
      heading="No job postings yet"
      body="Add job postings you've found."
    />
  )
}
```

`frontend/app/(dashboard)/documents/page.tsx`:
```typescript
import { EmptyState } from "@/components/shared/empty-state"

export default function DocumentsPage() {
  return (
    <EmptyState
      heading="No documents yet"
      body="Upload your CVs, cover letters, and other documents."
    />
  )
}
```
All heading and body copy matches the UI-SPEC copywriting contract exactly.
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt/frontend && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/layout/sidebar.tsx contains "export const navItems" and "hidden w-64 flex-col border-r md:flex"
    - frontend/components/layout/sidebar.tsx contains "Dashboard" and "Applications" and "Companies" and "Jobs" and "Documents"
    - frontend/components/layout/sidebar.tsx contains "JobHunt" (app name in sidebar header)
    - frontend/components/layout/sidebar.tsx contains "bg-accent text-accent-foreground" (active nav item)
    - frontend/components/layout/topbar.tsx contains "UserButton" and "Menu" and "Sheet" and "MobileNav"
    - frontend/components/layout/topbar.tsx contains "md:hidden" (hamburger only on mobile)
    - frontend/components/layout/topbar.tsx contains "h-14" (56px height)
    - frontend/components/layout/mobile-nav.tsx contains "navItems" and "onNavigate"
    - frontend/app/(dashboard)/layout.tsx contains "Sidebar" and "Topbar" and "h-screen"
    - frontend/app/(dashboard)/dashboard/page.tsx contains "Welcome to JobHunt"
    - frontend/app/(dashboard)/applications/page.tsx contains "No applications yet"
    - frontend/app/(dashboard)/companies/page.tsx contains "No companies yet"
    - frontend/app/(dashboard)/jobs/page.tsx contains "No job postings yet"
    - frontend/app/(dashboard)/documents/page.tsx contains "No documents yet"
    - `cd frontend && pnpm build` exits 0
  </acceptance_criteria>
  <done>Dashboard layout with responsive sidebar (desktop) and hamburger Sheet (mobile), topbar with UserButton, all 5 placeholder pages with correct empty state copy, pnpm build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify responsive layout, auth flow, and navigation</name>
  <what-built>Complete frontend shell with Better Auth authentication (register, login, logout), responsive sidebar layout, route protection, and placeholder pages.</what-built>
  <how-to-verify>
**Prerequisites:**
1. Start PostgreSQL: `docker compose up -d` (from project root)
2. Run backend to apply Flyway V7 migration: `./gradlew :backend:bootRun` (wait for startup, then stop with Ctrl+C -- or keep running)
3. Start frontend: `cd frontend && pnpm dev`
4. Open http://localhost:3000

**Test 1: Landing page (unauthenticated)**
- Verify you see "Track your job search" heading
- Verify "Start tracking jobs" and "Sign in" buttons are visible
- Expected: Clean centered layout with two CTAs

**Test 2: Registration flow**
- Click "Start tracking jobs" -- should navigate to /auth/register
- Verify a registration form appears (email, password fields)
- Create an account with a test email and password (min 8 chars)
- Expected: After registration, redirected to /dashboard (autoSignIn is enabled)

**Test 3: Authenticated layout**
- Verify sidebar appears on left with: Dashboard, Applications, Companies, Jobs, Documents
- Verify top bar appears with UserButton (avatar/user menu) on the right
- Verify Dashboard page shows "Welcome to JobHunt"

**Test 4: Navigation**
- Click each sidebar link: Applications, Companies, Jobs, Documents
- Verify each shows the correct empty state message
- Verify the active nav item is highlighted

**Test 5: Logout**
- Click UserButton in top bar, click "Sign out"
- Verify redirect to landing page

**Test 6: Login flow**
- Click "Sign in" on landing page
- Enter the credentials from Test 2
- Verify redirect to /dashboard

**Test 7: Route protection**
- Log out, then try navigating directly to http://localhost:3000/dashboard
- Verify redirect to /auth/login

**Test 8: Responsive (INFR-05)**
- Open Chrome DevTools (F12), toggle device toolbar
- Set viewport to 375px width (mobile)
- Verify sidebar is hidden
- Verify hamburger icon appears in top bar
- Click hamburger -- verify slide-over navigation appears with same links
- Click a link -- verify Sheet closes and page navigates
- Set viewport to 768px -- verify sidebar reappears, hamburger hidden
- Set viewport to 1280px -- verify full desktop layout

**Test 9: Dark mode**
- Change OS/system theme to dark mode
- Verify the app automatically switches to dark theme (dark backgrounds, light text)
- Change back to light -- verify it switches back
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && pnpm build` succeeds
- `cd frontend && pnpm test` passes (api-client tests from Plan 01)
- All 5 dashboard routes render with correct empty state content
- Sidebar has 5 nav items matching CONTEXT.md specification
- Top bar shows UserButton from Better Auth UI
- Mobile breakpoint (below 768px) hides sidebar and shows hamburger
- Human verification confirms full auth flow and responsive layout
</verification>

<success_criteria>
- Authenticated users see sidebar + topbar layout on all dashboard pages
- Sidebar shows Dashboard, Applications, Companies, Jobs, Documents with correct icons
- Active nav item highlighted with bg-accent
- TopBar has UserButton (avatar dropdown with sign-out)
- Mobile: sidebar hidden, hamburger opens Sheet with same nav
- All placeholder pages show correct empty state copy from UI-SPEC
- Route protection redirects unauthenticated users to /auth/login
- Full auth flow works: register, login, logout, route protection
- Responsive at 375px, 768px, 1280px viewports (INFR-05)
- Dark mode follows system preference
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-shell-auth-ui/07-03-SUMMARY.md`
</output>
