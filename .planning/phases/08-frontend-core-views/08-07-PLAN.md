---
phase: 08-frontend-core-views
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/(dashboard)/layout.tsx
  - frontend/components/layout/sidebar.tsx
  - frontend/components/layout/topbar.tsx
  - frontend/components/shared/data-table.tsx
  - frontend/app/(dashboard)/applications/page.tsx
  - frontend/components/applications/application-board.tsx
  - frontend/components/applications/application-list.tsx
  - frontend/components/applications/filter-bar.tsx
  - frontend/app/(dashboard)/companies/page.tsx
  - frontend/app/(dashboard)/jobs/page.tsx
  - frontend/app/(dashboard)/documents/page.tsx
  - frontend/app/(dashboard)/dashboard/page.tsx
  - frontend/components/ui/sheet.tsx
autonomous: true
gap_closure: true
requirements: [APPL-03, APPL-04]

must_haves:
  truths:
    - "App layout is fully responsive from mobile (320px) to desktop (1440px+)"
    - "Sidebar is hidden on mobile, accessible via hamburger menu in topbar"
    - "Data tables scroll horizontally on mobile instead of overflowing"
    - "Page headers stack vertically on mobile with full-width buttons"
    - "Dashboard metric cards stack in 2-column grid on mobile"
    - "All pages have appropriate padding on mobile (no content touching edges)"
    - "Detail panel (Sheet) is full-width on mobile, 75% capped at 32rem on desktop"
    - "Filter bar has no double padding (px-6 removed, layout handles padding)"
  artifacts:
    - path: "frontend/app/(dashboard)/layout.tsx"
      provides: "Responsive layout with mobile-safe main area padding"
      contains: "p-3 sm:p-6"
    - path: "frontend/components/shared/data-table.tsx"
      provides: "DataTable with horizontal scroll wrapper for mobile"
      contains: "overflow-x-auto"
    - path: "frontend/app/(dashboard)/applications/page.tsx"
      provides: "Applications page with responsive header"
      contains: "flex-col sm:flex-row"
    - path: "frontend/components/ui/sheet.tsx"
      provides: "Sheet with full-width on mobile, wider max on desktop"
      contains: "sm:max-w-lg"
  key_links:
    - from: "frontend/app/(dashboard)/layout.tsx"
      to: "frontend/components/layout/sidebar.tsx"
      via: "Sidebar hidden on md: breakpoint"
      pattern: "hidden.*md:flex"
    - from: "frontend/components/shared/data-table.tsx"
      to: "frontend/components/ui/table.tsx"
      via: "Table wrapped in overflow container"
      pattern: "overflow-x-auto"
---

<objective>
Make the entire application responsive on mobile devices. Fix layout, navigation, tables, cards, buttons, spacing, detail panel width, and filter bar padding to work well from 320px to 1440px+ viewports.

Purpose: Addresses UAT issue 4 (app is not responsive on mobile). The layout, tables, filter bars, page headers, detail panel, and sheet width need responsive breakpoints.
Output: All pages and shared components adapted with mobile-first responsive classes.
</objective>

<execution_context>
@C:/Users/balab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/balab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/CLAUDE.md

<interfaces>
From frontend/app/(dashboard)/layout.tsx:
```typescript
export default function DashboardLayout({ children }) {
  return (
    <div className="flex h-screen">
      <Sidebar />
      <div className="flex flex-1 flex-col">
        <Topbar />
        <main className="flex-1 overflow-auto p-6">{children}</main>
      </div>
    </div>
  )
}
// Main area has p-6 on all viewports -- too much padding on mobile
```

From frontend/components/shared/data-table.tsx:
```typescript
export function DataTable<TData, TValue>({ columns, data, onRowClick, pagination })
// Table is NOT wrapped in an overflow container -- overflows on mobile
```

From frontend/app/(dashboard)/applications/page.tsx:
```typescript
// Header: <div className="flex items-center justify-between px-6 pt-6">
// Board/List tabs + New Application button on same line -- overflows on mobile
```

From frontend/components/applications/filter-bar.tsx:
```typescript
// Container: <div className="flex flex-wrap items-center gap-4 px-6 py-3">
// Has px-6 hardcoded -- will double-pad with layout padding
// Search input uses ml-auto (works fine with flex-wrap)
```

From frontend/components/ui/sheet.tsx:
```typescript
// SheetContent: data-[side=right]:w-3/4 data-[side=right]:sm:max-w-sm
// On 375px mobile: 75% = 281px -- TOO NARROW for detail panel with 4 tabs
// The detail panel contains forms, notes, interviews, documents tabs
// Need full-width on mobile, capped at larger max on desktop
```

From frontend/app/(dashboard)/companies/page.tsx:
```typescript
// Header: <div className="flex items-center justify-between">
// Grid: <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
// Already responsive grid -- good
```

From frontend/app/(dashboard)/jobs/page.tsx:
```typescript
// Header: <div className="flex items-center justify-between">
// Filter: <Select> with w-[200px] -- fine on mobile
```

From frontend/app/(dashboard)/documents/page.tsx:
```typescript
// Header: <div className="flex items-center justify-between">
// Filter: <Select> with w-[160px] -- fine on mobile
```

From frontend/app/(dashboard)/dashboard/page.tsx:
```typescript
// Metrics grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 -- already responsive
// Status breakdown and recent activity: full-width cards -- fine
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix layout, data-table scrolling, filter-bar padding, and applications page responsive</name>
  <files>frontend/app/(dashboard)/layout.tsx, frontend/components/shared/data-table.tsx, frontend/app/(dashboard)/applications/page.tsx, frontend/components/applications/application-list.tsx, frontend/components/applications/filter-bar.tsx</files>
  <read_first>
    - frontend/app/(dashboard)/layout.tsx
    - frontend/components/shared/data-table.tsx
    - frontend/app/(dashboard)/applications/page.tsx
    - frontend/components/applications/application-list.tsx
    - frontend/components/applications/filter-bar.tsx
  </read_first>
  <action>
**1. Fix dashboard layout padding** in `frontend/app/(dashboard)/layout.tsx`:

Change the main element from:
```tsx
<main className="flex-1 overflow-auto p-6">{children}</main>
```
to:
```tsx
<main className="flex-1 overflow-auto p-3 sm:p-6">{children}</main>
```

This gives `12px` padding on mobile and `24px` on sm+ screens.

**2. Make DataTable horizontally scrollable** in `frontend/components/shared/data-table.tsx`:

Wrap the `<Table>` in a scroll container. Change:
```tsx
<div>
  <Table>
```
to:
```tsx
<div>
  <div className="overflow-x-auto">
    <Table>
```

And close the new div after `</Table>`:
```tsx
    </Table>
  </div>
```

This ensures tables scroll horizontally on mobile instead of overflowing the page.

**3. Make applications page header responsive** in `frontend/app/(dashboard)/applications/page.tsx`:

Change the header from:
```tsx
<div className="flex items-center justify-between px-6 pt-6">
  <h1 className="text-xl font-semibold">Applications</h1>
  <div className="flex items-center gap-3">
    <Tabs ...>
```
to:
```tsx
<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
  <h1 className="text-xl font-semibold">Applications</h1>
  <div className="flex items-center gap-3">
    <Tabs ...>
```

Remove `px-6 pt-6` from the header since parent layout now handles padding.

Also remove the hardcoded `px-6` from the error state div (line 70) and the empty state wrapper (line 95) and loading states (line 75, 88-91) since the layout padding now handles it. Change:
- `<div className="px-6 py-12 text-center text-sm text-muted-foreground">` to `<div className="py-12 text-center text-sm text-muted-foreground">`
- `<div className="px-6 py-8">` to `<div className="py-8">`
- Loading skeletons: remove `p-4` from board skeleton wrapper and `px-6` from list skeleton wrapper since layout provides padding

**4. Fix filter-bar.tsx double padding:**

In `frontend/components/applications/filter-bar.tsx`, remove the hardcoded `px-6` from the container since the parent layout now provides padding. Change:
```tsx
<div className="flex flex-wrap items-center gap-4 px-6 py-3">
```
to:
```tsx
<div className="flex flex-wrap items-center gap-4 py-3">
```

The `flex-wrap` is already present (good for mobile wrapping). The `ml-auto` on the search input works correctly with flex-wrap.
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt/frontend && grep "p-3 sm:p-6" app/\(dashboard\)/layout.tsx && grep "overflow-x-auto" components/shared/data-table.tsx && grep "flex-col.*sm:flex-row" app/\(dashboard\)/applications/page.tsx && grep -v "px-6" components/applications/filter-bar.tsx | grep "flex flex-wrap"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(dashboard)/layout.tsx main element has `p-3 sm:p-6`
    - frontend/components/shared/data-table.tsx has `overflow-x-auto` wrapper around Table
    - frontend/app/(dashboard)/applications/page.tsx header uses `flex-col gap-3 sm:flex-row sm:items-center sm:justify-between`
    - No hardcoded `px-6` padding remains in applications page content areas (layout handles it)
    - frontend/components/applications/filter-bar.tsx container has NO `px-6` (removed), still has `flex-wrap`
  </acceptance_criteria>
  <done>Layout has mobile-safe padding, tables scroll horizontally, applications page header stacks on mobile, filter bar has no double padding</done>
</task>

<task type="auto">
  <name>Task 2: Make remaining pages responsive (companies, jobs, documents, dashboard)</name>
  <files>frontend/app/(dashboard)/companies/page.tsx, frontend/app/(dashboard)/jobs/page.tsx, frontend/app/(dashboard)/documents/page.tsx, frontend/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - frontend/app/(dashboard)/companies/page.tsx
    - frontend/app/(dashboard)/jobs/page.tsx
    - frontend/app/(dashboard)/documents/page.tsx
    - frontend/app/(dashboard)/dashboard/page.tsx
  </read_first>
  <action>
**1. Companies page** (`frontend/app/(dashboard)/companies/page.tsx`):

The page header `<div className="flex items-center justify-between">` should become:
```tsx
<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
```
So on mobile, the title stacks above the "Add Company" button (button goes full width on mobile). Add `w-full sm:w-auto` to the Add Company Button:
```tsx
<Button onClick={handleAdd} className="w-full sm:w-auto">
```

The company card grid already has `grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3` which is responsive -- no change needed.

**2. Jobs page** (`frontend/app/(dashboard)/jobs/page.tsx`):

Same header pattern:
```tsx
<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
  <h1 className="text-xl font-semibold">Jobs</h1>
  <Button onClick={handleAdd} className="w-full sm:w-auto">
```

The filter row `<div className="flex items-center gap-4">` -- add responsive classes:
```tsx
<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:gap-4">
```
And make the Select full-width on mobile:
```tsx
<Select value={companyFilter} onValueChange={handleCompanyChange}>
  <SelectTrigger className="w-full sm:w-[200px]">
```

**3. Documents page** (`frontend/app/(dashboard)/documents/page.tsx`):

Header pattern:
```tsx
<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
  <h1 className="text-xl font-semibold">Documents</h1>
  <Select value={categoryFilter} onValueChange={setCategoryFilter}>
    <SelectTrigger className="w-full sm:w-[160px]">
```

**4. Dashboard page** (`frontend/app/(dashboard)/dashboard/page.tsx`):

The metrics grid already uses `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` -- good.

Change the metric card value font size to be slightly smaller on mobile:
```tsx
<span className="text-2xl font-semibold leading-none sm:text-[28px]">
```

The status breakdown items and recent activity items -- add `text-xs sm:text-sm` to make text slightly smaller on mobile for the status label and recent activity text. Specifically:
- In the byStatus map, change `<span className="text-sm">{label}</span>` to `<span className="text-xs sm:text-sm">{label}</span>`
- In the recent activity map, change `<p className="text-sm font-medium">{app.jobTitle}</p>` to `<p className="text-xs font-medium sm:text-sm">{app.jobTitle}</p>`

These are subtle but help on very small screens (320px).
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt/frontend && grep "flex-col.*sm:flex-row" app/\(dashboard\)/companies/page.tsx && grep "flex-col.*sm:flex-row" app/\(dashboard\)/jobs/page.tsx && grep "flex-col.*sm:flex-row" app/\(dashboard\)/documents/page.tsx && grep "sm:text-\[28px\]" app/\(dashboard\)/dashboard/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(dashboard)/companies/page.tsx header uses `flex-col gap-3 sm:flex-row sm:items-center sm:justify-between`
    - frontend/app/(dashboard)/companies/page.tsx Add Company button has `w-full sm:w-auto`
    - frontend/app/(dashboard)/jobs/page.tsx header uses same responsive pattern
    - frontend/app/(dashboard)/jobs/page.tsx filter Select has `w-full sm:w-[200px]`
    - frontend/app/(dashboard)/documents/page.tsx header uses same responsive pattern
    - frontend/app/(dashboard)/documents/page.tsx category Select has `w-full sm:w-[160px]`
    - frontend/app/(dashboard)/dashboard/page.tsx metric value uses `text-2xl sm:text-[28px]`
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>All pages have responsive headers that stack on mobile, buttons go full-width, selects adapt, metric text scales</done>
</task>

<task type="auto">
  <name>Task 3: Widen Sheet (detail panel) for mobile usability</name>
  <files>frontend/components/ui/sheet.tsx</files>
  <read_first>
    - frontend/components/ui/sheet.tsx
  </read_first>
  <action>
The SheetContent component is used for the application detail panel which has 4 tabs (Overview, Notes, Interviews, Documents). On mobile, the current `w-3/4` gives only 281px on a 375px screen — forms and tab content are unusable at that width.

Modify `frontend/components/ui/sheet.tsx` SheetContent className:

Change the right-side width from `data-[side=right]:w-3/4` to `data-[side=right]:w-[92%]` and change `data-[side=right]:sm:max-w-sm` to `data-[side=right]:sm:max-w-lg`.

Also apply the same change for left-side sheets: `data-[side=left]:w-3/4` → `data-[side=left]:w-[92%]` and `data-[side=left]:sm:max-w-sm` → `data-[side=left]:sm:max-w-lg`.

This gives:
- Mobile: 92% of viewport (345px on 375px screen — usable for forms and tabs)
- Desktop: capped at 32rem (512px) — plenty of room for detail content

The 8% gap on mobile keeps the overlay visible as a visual cue that the user can close via the X button (tap-outside is blocked by the dialog fix in 08-06).
  </action>
  <verify>
    <automated>cd D:/dev/projects/job-hunt/frontend && grep "w-\[92%\]" components/ui/sheet.tsx && grep "sm:max-w-lg" components/ui/sheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/ui/sheet.tsx has `data-[side=right]:w-[92%]` (not w-3/4)
    - frontend/components/ui/sheet.tsx has `data-[side=right]:sm:max-w-lg` (not sm:max-w-sm)
    - frontend/components/ui/sheet.tsx has matching changes for left-side sheets
    - Sheet still slides in/out correctly (animations unchanged)
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Detail panel is 92% width on mobile (345px on 375px screen) and up to 512px on desktop — usable for all 4 tabs</done>
</task>

</tasks>

<verification>
- Resize browser to 320px width: all pages render without horizontal overflow
- Page headers stack title above action buttons on mobile
- Data tables in jobs and applications list views scroll horizontally on mobile
- Dashboard metrics show in 1-column on narrow mobile, 2-column on wider mobile
- Company cards show 1-column on mobile, 2 on tablet, 3 on desktop
- Detail panel (Sheet) fills 92% of mobile screen width
- Filter bar has no double horizontal padding
- `pnpm build` succeeds
</verification>

<success_criteria>
1. No horizontal page overflow on any page at 320px viewport width
2. All page headers stack vertically on mobile
3. Tables have horizontal scroll containers
4. Layout padding reduces to 12px on mobile
5. Sheet detail panel is 92% on mobile, max 32rem on desktop
6. Filter bar px-6 removed (no double padding)
7. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-core-views/08-07-SUMMARY.md`
</output>
