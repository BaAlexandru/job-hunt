apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: jobhunt-prod
spec:
  limits:
    - type: Container
      default:
        memory: 256Mi
        cpu: 250m
      defaultRequest:
        memory: 128Mi
        cpu: 100m
      max:
        memory: 512Mi
        cpu: "1"
